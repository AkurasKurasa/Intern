[pytest]
testpaths = tests
addopts =
    --ignore=tests/test_cv_pipeline.py
    --ignore=tests/test_html_detector.py
    --ignore=tests/test_transformer_bc.py
    --ignore=tests/test_two_state_recording.py
# The four ignored files predate 2026-08-06 and fail to even import:
#   test_cv_pipeline.py / test_two_state_recording.py -> trace_translator.trace_translator
#     imports a class (HTMLDetector) that no longer exists anywhere in the package.
#   test_html_detector.py -> imports trace_translator.html_detector, a module that
#     doesn't exist.
#   test_transformer_bc.py -> imports components.intelligence.model.dataset/model/
#     train/predict, a module split that no longer exists (consolidated into
#     components/intelligence/model/transformer.py at some point without updating
#     this test). Needs a real rewrite against the current module, not a quick patch.
