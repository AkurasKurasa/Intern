# Intern — full dependency set (Windows only; UIA/win32/pyautogui are Windows-specific).
# Install with: pip install -r requirements.txt
# Also needs Tesseract OCR installed system-wide (not pip-installable):
#   winget install --id UB-Mannheim.TesseractOCR
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki

# GUI automation / control
pyautogui>=0.9.54
pywin32>=306
uiautomation>=2.0.20
pynput>=1.7.6
wxPython>=4.2.0

# Perception (CV + OCR)
opencv-python>=4.8.0
pytesseract>=0.3.10
mss>=9.0.0
Pillow>=10.0.0

# Learning (BC transformer)
torch>=2.0.0
sentence-transformers>=2.2.0
numpy>=1.24.0

# LLM providers (lmstudio needs no key; others need one in .env)
openai>=1.0.0

# Inbox Router (Scope #3) -- Gmail API + OAuth2 for RealGmailClient.
# anthropic / google-genai added here too: llm_classifier.py wants
# Claude/Gemini and neither is a hard requirement elsewhere in this repo --
# agent.py only ever soft-imports them (falls back to transformer-only if
# absent). groq deliberately NOT added as its own package: the
# already-installed openai package reaches Groq's OpenAI-compatible
# endpoint directly (same trick this project already uses for LM Studio).
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
anthropic>=0.40.0
google-genai>=0.3.0

# Browser automation — Scope #2's executor/scanner drives the mock portal
# through Playwright, and Scope #3's automate_inbox/automate_cold_email drive
# the practice inbox the same way. Both were missing here: a fresh clone that
# installed exactly this file got an ImportError the moment it ran either
# scope. The browser binary is a separate, non-pip step:
#   python -m playwright install chromium
playwright>=1.40.0

# Spreadsheets — Scope #2 reads the grade sheet and records a live Excel
# demonstration. Also missing here previously; components/scope2/README.md
# listed them in its own pip line instead, so only someone reading that file
# ended up with a working Scope #2.
pandas>=2.0.0
openpyxl>=3.1.0
xlwings>=0.30.0

# Metrics — statistical significance testing (scripts/compare_baseline.py)
scipy>=1.10.0

# Tests
pytest>=8.0.0
