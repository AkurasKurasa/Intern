// treetask data — edit this file, refresh index.html (`make tree`).
// PRINCIPLE: every node is an OUTCOME you can check ("the form fills itself"),
// not a mechanism ("ranked arbitration"). Mechanisms live in notes/children.
// Team (owner values): Andrei | Ralph | Daniel | Kevin | unassigned
// Node: { id?, title, status, note?, owner?, priority?, deps?: [ids], children? }
//   status:   "done" | "partial" | "next" | "pending" | "risk"
//   priority: integer — shows in the priority table, lower = higher
//   deps:     ids that must be DONE before this can start (⛓ = blocked)
const TREE = {
  id: "root",
      owner: "Ralph",
  title: "Complete Intern",
  status: "partial",
  note: "An agent that learns GUI work by WATCHING a person do it — then does it alone, their way. No hardcoded rules anywhere. Done = all three proof-scopes pass + thesis written.\n\nWhy it beats the alternatives: scripted RPA breaks on any change and knows no user; generic computer-use agents (incl. Codex Record & Replay) generalize a procedure but clone nobody. Intern's claims: personalization, local execution, judgment cloning.",
  children: [
    {
      id: "scope1",
      owner: "Ralph",
      title: "SCOPE 1 · Form Filling (the form fills itself)",
      status: "partial",
      note: "Proof scope #1 (car-insurance form, 8 tabs). DONE means: all 5 intake records entered and submitted in the demonstrated order, unattended, with numbers to prove it.",
      children: [
        {
          id: "full-8-record-run",
          title: "Full 8 Record run",
          status: "partial",
          note: "All Scope 1 workstreams, grouped under the single end-to-end target: one full 8-tab run across all records, hands off, numbers trusted.",
          children: [
        {
          id: "acceptance-run",
          title: "One record, end to end, hands off",
          status: "done",
          deps: ["v3-model", "sections-work", "widgets-fill"],
          note: "THE GATE — PASSED 2026-07-10 evening. One uninterrupted run on v3: tab 0 → all 8 tabs → verify → autonomous Submit, zero human touches, whole week's stack live together. Earlier same-evening attempt wedged in the jump ping-pong (~step 180, Drivers); density gate + viewport lock + far-field reveal landed, rerun submitted. Caveat: metrics block not archived — capture the scorecard on the next run.",
        },
        {
          id: "trust-numbers",
          owner: "Ralph",
          title: "The score can be trusted",
          status: "done",
          note: "DONE 2026-07-12 — all four sub-fixes landed and offline-verified: record-aware gold (both scorers), section-aware gold keys, and full 8-tab coverage (bc-gold-coverage, the last piece). Run scorer (eval_metrics) section-fix 2026-07-10 (41cff4c). bc_fidelity FIXED 2026-07-11/12: honest rescore of the ×2 run = record 1: 89.3% field / 100% value / 0 mismatches; record 2: 63.5% / 100% (was 20%/32.6% fiction) — now against the FULL 163-field gold, not 75. Every scorer number quoted from here on is trustworthy.",
          children: [
            { id: "bc-record-gold", title: "bc_fidelity grades each record vs ITS OWN answers", status: "done",
              note: "FIXED 2026-07-11: _detect_record_num matches the submission's policy number against each intake record (data-driven), rebuilds gold for that record on the fly; static ref = flagged fallback; report prints which gold was used. Verified: 00441→record 1, 00442→record 2." },
            { id: "bc-section-gold", title: "bc_fidelity gold keys are section-aware", status: "done",
              note: "FIXED 2026-07-11: _parse_intake_record tracks [Section] headers — bare labels inside [Driver N]/[Vehicle 2+] never map to ph_*/v_* keys (Driver 3's 'Tyler' had overwritten ph_first); first-occurrence-wins belt; intake annotations ('← NOTE', '[VERIFY —…]') stripped from gold values." },
            { id: "bc-gold-coverage", title: "BC gold covers the whole form, not 3 tabs", status: "done",
              note: "FIXED 2026-07-12, offline-verified against real submissions. _LABEL_TO_KEY extended (~90 new entries, sourced from a real submission JSON's actual keys, not guessed) to Coverage/History/Discounts/Claims/Payment; _TAB_PREFIXES gained hist_/disc_ (had no tab name at all before). Driver 2/3 needed section-aware suffix mapping (same bare labels as Policyholder, e.g. 'First Name' -> d2_first/d3_first) via new _DRIVER_LABEL_TO_SUFFIX; genuinely ambiguous labels ('City'/'State'/'ZIP', 'Total Premium ($)' appearing in two different sections with different keys) got an explicit (section,label) override table checked first. Bonus fix found mid-verification: '(leave blank)' was leaking through as LITERAL gold text (not skipped like '(none)'/'n/a') — normalized the same way the agent's own lookup does. RESULT: record 1 gold 75->163 fields, tab coverage 33%->100% on both real archived submissions. Residual (cosmetic, not chased): a few Policy-tab keys with no 'policy_' prefix (agent_id, underwriter...) bucket into an 'Other' display label — doesn't affect scoring correctness." },
            { id: "eval-record-gold", title: "Run scorer knows which record it's grading", status: "done",
              note: "FIXED 2026-07-11: evaluate_run takes record_num from run_task (which KNOWS which record ran) — no more majority-vote inference mis-picking record 1 (shared generic values + record 1's larger field count biased the vote; ties defaulted to record 1). Inference kept as fallback for callers without the number. Offline-verified against the exact live failure: contamination-biased run scored ✗ 'expected Marcus D. Chen' via inference, ✓ via record_num=2." },
          ],
        },
        {
          id: "sections-work",
          title: "Repeated sections fill correctly (Driver 2/3)",
          status: "done",
          note: "Was: whole sections silently skipped — same-named fields ('First Name' ×3) collided in every skip-list. Fixed 07-09: identities are (section, label); sweep looks up section-correct values. Live-verified on the Drivers tab.",
        },
        {
          id: "widgets-fill",
          title: "Every widget type can be filled",
          status: "partial",
          note: "No more dead fields. Backbone = the identity executor: command controls directly through Windows' accessibility API (set value / toggle / select) instead of clicking coordinates and pasting.",
          children: [
            { id: "identity-core", title: "Direct control API built + proven (edits, spins, checkboxes, combos)", status: "done",
              note: "_act_on_element: ValuePattern/TogglePattern/SelectionItem. Live-verified on Drivers." },
            { id: "identity-everywhere", owner: "Ralph", title: "ALL fill paths use ONE pipeline (pixels last)",
              status: "done", priority: 4, deps: ["identity-core", "acceptance-run"],
              note: "INVERSION LANDED 2026-07-11, FULL ACCEPTANCE PASSED 2026-07-13: new _fill_element = THE fill pipeline (reliable mechanics first, pixels last, read-back verified) proven end-to-end — all 8 tabs, verify pass, deterministic Submit, 90 steps, zero fill-pipeline drama (no dropdown escapes, no rescue loops beyond the known spin-visibility hiccup which self-resolved every time). LLM fill-decisions 2.0% (was ~45-55%) — the deterministic short-circuit's structural win, confirmed on a full run, not just a drill. Value accuracy 100% (run scorer) / 96.2% (163-field BC gold). All three combobox paths + reveal-focus route through the pipeline; rescue copies deleted; OPT2 edit typing keeps its deliberate paste→keystroke→pattern ladder. REMAINING (split out, not blocking): verify-fix read-back unification, retire snap/stale-coord guards, and the Driver-2-overwrite bug (see driver-section-crosstalk — REOPENED same day, regressed at a different call site)." },
            { id: "dead-widgets", title: "Stubborn cases: long dropdowns + paste-reject spins", status: "done",
              note: "DONE 2026-07-11, verified three ways: probe (scratch/probe_dead_widgets.py — spin ''→'3' via ValuePattern/RangeValue; 'State' ''→'Texas' below-fold, winner = FIRST-LETTER CYCLING with read-back, wx.Choice matches single chars); drills 22:44 + 23:08 (both 50-state widgets ~1s each, all three combobox paths through the pipeline); spin rescue live 22:49. User caught mid-way that a drill alone wasn't proof AND that rescues-after-pixel-failure were a band-aid — both led to the pipeline inversion. Residual split out → spin-visibility." },
            { id: "driver-section-crosstalk", owner: "Ralph", title: "Driver 2/3 don't get overwritten with Driver 1's values", status: "done",
              note: "REOPENED 2026-07-13, FIXED + LIVE-CONFIRMED same day. First instance (verify-pass path, off-screen stale bboxes in _view_mismatches) fixed that morning. REGRESSED at a different call site (_auto_fill): reproduced on a naturally-terminated, 100%-complete rerun (d2_dl_exp expected '03/22/2027' got '07/14/2028'). ROOT CAUSE: _detect_section returned '' for 'Driver 2 DL Expiration' (last field in the block, right before [Driver 3] starts) because the Driver 2 section-pane element was entirely absent from state at that scroll position — not stale, just gone. FIX: sticky-section fallback in _detect_section (agent.py:3782-3810) — remembers the last non-empty section, uses it when no pane is above the field but section panes exist SOMEWHERE in view; resets to '' when a view has zero section panes anywhere. LIVE-CONFIRMED on the very next rerun: d2_dl_exp gone from BC top-mismatches. (A DIFFERENT instance of the same family immediately surfaced in that same rerun — see verify-pass-wrong-twin.)" },
            { id: "verify-pass-wrong-twin", owner: "Ralph", title: "Verify pass can fix the wrong twin among repeated-section fields", status: "partial",
              note: "FOUND 2026-07-13, same rerun that confirmed the driver-crosstalk fix: d2_violations expected '0' got '1' — Driver 2's own correct value got overwritten with Driver 3's. ROOT CAUSE (confirmed by reading the code): _verify_pass resolves the SPECIFIC wrong element via _view_mismatches (section-aware) but then calls _nav_fill_field with only the bare label, no prefer_key — _nav_fill_field's own _find() re-resolves from scratch and its fallback ('exact label, prefer empty') picks the FIRST same-labeled candidate by list order once BOTH twins are non-empty (Driver 2/3's comboboxes share the bare label 'Violations (3 yr)', same as DL Expiration — these widgets never carry a driver number in their own UIA label). FIX: both verify-pass fill call sites (agent.py ~5667, ~5752) now pass prefer_key — the deterministic branch reuses the _vk it already computed, the LLM-fallback branch computes one fresh via _attempt_key. Syntax-checked. OFFLINE-VERIFIED (scratch/probe_verify_wrong_twin.py — copies the exact _find()/_attempt_key algorithm): reproduces the bug with prefer_key=None (resolves to Driver 2's widget), confirms the fix with prefer_key set (resolves to Driver 3's). LIVE RETEST (--start_tab 4 drill, interrupted intentionally before Submit): actual trace shows Driver 2 DL Expiration/Violations AND Driver 3 DL Expiration/Violations/Relationship/DL Number all filled section-correctly — no crosstalk observed. BC SCORE printed for that run was stale leftover data (see bc-fidelity-stale-submission) so not a formal confirmation. FULL UNINTERRUPTED RERUN still scored d2_violations wrong — traced to a REGRESSION in the sticky-section fix itself (see sticky-section-order-bug): prefer_key correctly pinned the fix to the right widget, but the section lookup handed it the wrong expected value." },
            { id: "sticky-section-order-bug", owner: "Ralph", title: "Sticky-section fallback was order-dependent (v1 regression, fixed v2)", status: "partial",
              note: "FOUND 2026-07-13: full uninterrupted rerun (100% completion) still scored d2_violations wrong even with prefer_key fixed. ROOT CAUSE: the sticky-section fallback's v1 used ONE scalar (_last_nonempty_section) shared across every field in a scan. _view_mismatches iterates elements in whatever order the observer returns them (not guaranteed top-to-bottom) — if Driver 3's own field got processed before Driver 2's off-screen-pane straggler field in the same scan, the scalar got overwritten to 'Driver 3' first and then wrongly answered for Driver 2's field too. FIX (v2): replaced the scalar with a per-pane remembered-top dict (_section_pane_tops) — each field judged against the pane it actually belongs to by remembered geometry, order-independent. Reset at all 11 tab-switch clear() sites. OFFLINE-VERIFIED (scratch/probe_section_order_independence.py): both processing orders resolve correctly. THIRD INSTANCE found on the very next rerun (see section-unknown-vs-confirmed) — v2's fix held (d2_violations gone) but a related gap in the SAME function got exposed next." },
            { id: "section-unknown-vs-confirmed", owner: "Ralph", title: "Section detection conflated 'no sections here' with 'haven't looked yet'", status: "partial",
              note: "FOUND 2026-07-13, third instance of the same crosstalk family: after v2's order-independence fix held (d2_violations gone), the SAME rerun showed d3_first/d3_dob/d3_relation all wrong — Driver 3's fields got the POLICYHOLDER's bare-keyed values (First Name 'James', DOB '07/14/1978') and Driver 2's fuzzy-matched value (Relationship 'Spouse'). ROOT CAUSE: _detect_section returned '' both when a field is confirmed above all known sections (safe to bare-lookup) AND when NO section pane has been observed yet this tab-visit. FIX (v3): return None for the 'zero panes remembered yet' case, defer instead of guessing. RETESTED 2026-07-14: still broke, same values — v3's None-check never triggered because _section_pane_tops held STALE remembered coordinates from an earlier scroll position, not zero. Superseded — see structural-section-detect." },
            { id: "structural-section-detect", owner: "Ralph", title: "Structural section detection: tried, reverted (form's widget tree is flat)", status: "done",
              note: "TRIED 2026-07-14: rewrote _detect_section to read a real UIA ancestor-pane chain (tagged by the observer at walk-time) instead of inferring section membership from screen geometry. Fixed d3_first/d3_dob/d3_relation on the next live rerun — but caused a WORSE regression: ALL FIVE of Driver 2's fields came back as Driver 1's values. ROOT CAUSE OF THE REGRESSION: read car_insurance_form_wx.py's actual construction — the section-header banner and every field in that section are ALL direct siblings under the same tab page (_form_grid returns a layout SIZER, not a container); there is NO parent-child relationship between a section and its fields on this form. The structural assumption simply didn't hold here. REVERTED _detect_section back to geometry; the observer's ancestor_panes tagging is left in place, harmless/unused, for a future scope where sections really are nested. See section-revisit-stale-fix for what the REAL remaining bug turned out to be." },
            { id: "section-revisit-stale-fix", owner: "Ralph", title: "The actual gap: verify pass never reset section memory on tab re-visit", status: "done",
              note: "FOUND 2026-07-14 while reverting the structural attempt: _verify_pass's own tab-switch loop manages tab-clicking independently from the rest of the agent and was NEVER among the 11 _filled_this_tab.clear() sites that got _section_pane_tops = {} added earlier — a 12th call site, missed because it doesn't share that clear() call. This exactly explains why v3/v4 kept failing: the main pass visits Drivers tab and remembers pane positions at ITS scroll state; verify later re-clicks the SAME tab (different scroll position) and, without a reset, judges fields against STALE coordinates from the earlier visit — Driver 3's fields got Policyholder's/Driver 2's values. FIX: added the missing _section_pane_tops = {} reset at the top of verify_pass's tab-switch loop. OFFLINE-VERIFIED (scratch/probe_verify_revisit_stale.py). LIVE-CONFIRMED 2026-07-14: next uninterrupted rerun (100% completion) produced exactly ONE BC mismatch total (d3_dob, the separate already-known zero-pad bug) — zero crosstalk of any kind across Driver 1/2/3. Fifth round on this bug family; this is the one that closed it." },
            { id: "bc-fidelity-stale-submission", owner: "unassigned", title: "BC scorer no longer re-grades a stale leftover submission", status: "done",
              note: "FOUND + FIXED 2026-07-13: an intentionally-interrupted --start_tab 4 drill (Ctrl+C at step 11, before Submit) still printed a full BC SCORE — but it was byte-identical to the PREVIOUS run's mismatches, because score_run just grabs the newest file in SUBMISSIONS_DIR with no check that THIS run produced one. Confirmed via timestamps: the graded submission predated the drill's own start. FIX: score_run gained run_start_ts; run_task.py captures time.time() per record and passes it through; a submission older than that is refused with a loud '[BC Fidelity] STALE' message instead of silently re-scored. Syntax-checked; not yet live-retested." },
            { id: "spin-visibility", owner: "unassigned", title: "Filled spin values visible to the observer", status: "partial",
              note: "FIX LANDED 2026-07-13. Found 22:49: identity-executor write LANDS but OBSERVED value stays '' → re-targets filled field (~15 wasted steps) → wrongly dead-marked. ROOT CAUSE narrowed past the ranked-picker (already masks correctly via _filled_this_tab) to a SEPARATE code path: OPT2 'focused empty field' direct-fill (agent.py ~1950-1977) decides fill-vs-navigate purely from the focused element's observed value, never checked _filled_this_tab. FIX: guard added — build same section-qualified key the dead-widget rescue itself writes (_detect_section + label), skip auto-fill if already in _filled_this_tab, trust the write over a stale observer read. Syntax-checked. PARTIALLY LIVE-RETESTED same day: a `--records 1` rerun did NOT terminate cleanly (max_steps, 27.3% completion, no_change streaks ~106-141) — consistent with a stall but NOT confirmed to be this bug vs. the driver-crosstalk regression or the DOB-zero-pad bug found in the same run. Needs a clean rerun with [OPT2]-tagged debug logging to confirm the guard fires." },
            { id: "dob-zero-pad", owner: "Ralph", title: "DOB submitted without leading zero", status: "done",
              note: "ROOT-CAUSED + FIXED 2026-07-14, NOT A CODE BUG: read the LIVE Notepad window's actual text buffer directly (no agent run needed) and found the window title is '*data_entry_intake.txt - Notepad' (asterisk = unsaved changes) with the live buffer's Date of Birth line literally reading '9/05/2006' — missing the zero IN THE OPEN DOCUMENT, while the file on disk is correct. A stray keystroke during the ~15+ reused-window live runs that day silently dropped one character, never saved. Every code-side theory (record cache, LLM reformat, keystroke-drop-on-paste) was chasing a phantom. FIX: targeted single-character insert directly into the live Edit control via EM_SETSEL+EM_REPLACESEL at the exact computed offset (verified unique match first, verified +1 char/same line count after — nothing else in the unsaved buffer touched). LIVE-CONFIRMED end-to-end: next Drivers-tab drill's log shows 'Deterministic value: Date of Birth -> 09/05/2006 from record 1' and the typed value scored 09/05/2006 exactly correct." },
            { id: "early-termination", owner: "unassigned", title: "RUN METRICS mislabels a genuine Submit as early-terminated", status: "done",
              note: "RETRACTED then FIXED 2026-07-13 — user caught the misdiagnosis: the run DID reach Submit (BC_SCORE created a real submission JSON + ran RuleExtractor, both post-submit-only), it just PRINTED 'Terminated: early/max_steps' / 27.3% completion. ROOT CAUSE: evaluate_run's done-check only looked for an action_type=='done' sentinel; Submit fires via a CLICK on the Submit button and never emits that sentinel, and run_task.py never passed agent._submitted in at all. FIX: evaluate_run gained a submitted param, done = bool(submitted) or the old scan; run_task.py now passes agent._submitted. tcr already keyed off done, so one fix corrects both the label and the completion %. Syntax-checked; not yet live-retested." },
            { id: "auto-log-to-disk", owner: "unassigned", title: "run_task.py always writes a full plain-text log to disk", status: "done",
              note: "BUILT 2026-07-13 — user asked to make log-to-file a default so it can't be forgotten (was previously manual stdout redirection). run_task.py:27-74: a _Tee wraps sys.stdout/stderr right after the UTF-8 reconfigure — mirrors every write to console (ANSI intact) AND to data/output/run_logs/run_<timestamp>.txt (ANSI stripped, stays plain/greppable). Deliberately NOT a logging.FileHandler: RUN METRICS/BC SCORE use raw print(), which a FileHandler attached only to the logging module would miss entirely — the Tee catches both. Tee logic unit-verified standalone. LIVE-CONFIRMED: two subsequent user-run acceptance runs both auto-wrote to data/output/run_logs/, both read in full by Claude with zero truncation." },
          ],
        },
        {
          id: "model-drives",
          title: "The learned model does the driving (LLM < 5%)",
          status: "partial",
          note: "Today the LLM assists ~45-50% of steps. The thesis claim needs the TRAINED policy driving nearly everything; LLM = values only.",
          children: [
            { title: "Navigation: fill batch → jump to densest work → repeat", status: "done",
              note: "Navigation Protocol v2: model's own ranked targets, visible-first; viewport jumps anchored on the model's next pick. Replaced the guard pile.\n\nPing-pong regression (2026-07-10, two anchors alternating 14 jumps / zero fills) fixed same day and CONFIRMED by the passing acceptance run: density gate + viewport lock (no re-jump since last progress) + far-field reveal (focus the window's far-side field — whole window on screen in one SetFocus, no ScrollPattern). Offline probe: scratch/probe_jump_pingpong.py (5/5)." },
            { title: "Model speaks in intents, not raw clicks", status: "done",
              note: "Universal Semantic Action Space: verbs (set-value/toggle/select/…) + split pointer heads. v2 beat baseline 0.957 vs 0.878." },
            { id: "alt-llm", title: "Find alternative LLM", status: "pending",
              note: "Evaluate replacements for LM Studio 'local-model' as the WHAT-provider. Pain: 2-5s/call latency, wrong-line grabs, JSON drift. DO: benchmark candidates (newer Qwen/Llama/Phi locals, or the wired Groq path) on the same prompts — value accuracy vs intake, latency, format reliability. Right-size: the deterministic-lookup fix shrinks how much this matters." },
            { id: "verb-loop", owner: "Ralph", title: "Agent acts on the model's intents directly", status: "pending",
              priority: 3, deps: ["acceptance-run"],
              note: "predict() already outputs verbs; the agent still converts to legacy click/type dicts. Wiring verbs → identity executor is what collapses LLM dependency and retires the remaining navigation crutches.\n\nLLM-45% ATTACK PLAN:\n1. [DONE — LIVE-VERIFIED 23:08 drill] Deterministic value short-circuit: every value 'no LLM call', fields landing ~2s apart (was ~7s), ONE HTTP call in the whole stretch (the GAP tab-advance). Design rule settled with user: look up what's look-up-able, think only about what isn't — LLM's jobs = ambiguity, judgment (Scope 3), rule inference.\n2. [BUILT 2026-07-14, see sweep-deterministic-nav] Deterministic next-tab + sweep value resolution — the sweep's own navigation call was the dominant LLM consumer (59-121 calls/record on the ×10 run).\n3. Verb-loop (this node) — verbs+pointer straight into the identity executor; makes <5% structural.\nHonest accounting (398d30c): 'LLM Calls (actual HTTP)' metrics line counts ALL calls incl. nav/sweep/verify — the tag-based 0.0% only covered fill-decisions.\n\nTASK-LIST ITEM #5 for 2026-07-15: SCOPE this properly first (a real plan — which call sites convert, what breaks, how to test incrementally), do NOT start writing code for it yet. This is the real path from ~13 min/record down to under a minute, but it's a genuine architectural rewrite, not a quick patch — needs design before implementation, same lesson as the strip-where-crutches core-loop-timing finding.\n\nSCOPE DELIVERED 2026-07-16 (chat, not yet a written design doc): WHAT this is — today the trained model already predicts a verb+pointer each step (semantic_action.py, v2 click_acc 0.957/src_acc 0.856) but the agent doesn't act on it; 'Option B' derives fill-vs-navigate from the focused widget's raw type INSTEAD of the verb (documented reason: the verb/action-type head is unreliable), and a parallel LLM-driven system (_navigation_protocol, sweep, verify's LLM-fallback, _merge's override rules) makes the real navigation/action decisions, with this session's deterministic short-circuits bolted on top of THAT system rather than replacing it. TARGET: agent executes the model's predicted verb directly (SemanticAction.to_legacy_dict() already exists for this) every step; LLM's only job shrinks to supplying SET_VALUE/SELECT_OPTION values when the record lookup is genuinely ambiguous; sweep/verify's separate LLM-navigation call sites retire structurally, not just short-circuit. RISK, stated plainly: every 'small' patch this session (tab-lock, _merge override, verify shortcircuit) surfaced a DEEPER bug on live test despite drilling first (5+ scattered tab-click sites, a silently-dropped value, a missing retry cap) -- a verb-loop rewrite replaces the ENTIRE per-step decision spine (_navigation_protocol + sweep + _merge + OPT2 gate + verify fallback), all of which carry non-obvious bookkeeping (_dead_fill_keys/_attempted_keys/_verify_fix_count/_verify_dead_stable/_fixation_hits/_filled_this_tab/_section_pane_tops) built up specifically to survive this codebase's known failure modes -- a rewrite risks silently dropping protections that exist only as inline comments in the code being replaced. STAGED PLAN (order, not timeline): (1) audit -- enumerate every piece of that bookkeeping state and decide, per item, keep/replace/drop, as its own deliverable before any code; (2) shadow-run the verb-loop decision alongside the current loop on a live record (log what it WOULD do, don't act on it), diff against actual behavior; (3) cut over ONE verb at a time, safest first (TOGGLE, binary/easy to verify), highest-value next (SET_VALUE, most of the current LLM-45%); (4) keep the existing verify pass as a safety net until the verb-loop proves it doesn't need rescuing on a real ×10 run; (5) re-run ×10 as the acceptance bar, same as every other structural change this session. OPEN QUESTION handed back to the user, not yet answered: is the goal here proving the thesis claim (LLM<5%, research/demo milestone) or making the agent faster/more robust for real use -- these point to different priorities; if it's the thesis claim the staged shadow-run approach above is right, if it's pure speed the already-landed session patches (verify-llm-fallback-shortcircuit etc.) get most of the win with far less risk and the full rewrite may not be worth it right now.\n\nSTAGE 1 (AUDIT) STARTED 2026-07-16, USER CALL ('let's do the biggest levers'): enumerated every self._[...] bookkeeping variable declared in __init__, cross-referenced actual call sites (not just definitions) for each. BIG FINDING, immediately actionable: self._verify_pass has ZERO call sites anywhere in the file -- confirmed by grep, not assumption -- meaning it's been 100% dead code since the earlier same-day 'no revisit, ever' change to _confirm_finished removed the only place that ever called it. That makes _view_mismatches (its only caller-of), self._verify_fix_count, self._verify_dead_stable, and self._verify_clean_tabs ALSO fully dead (write-only or entirely unreachable). WORSE: self._verified_at_fill was being WRITTEN every single step by the main-loop chokepoint (added earlier the same day) via a FULL UIA tree-walk (_field_matches -> _live_field_value -> _resolve_live_control) -- real, measurable per-step cost -- to populate a set that _view_mismatches (the only reader) can never reach since _verify_pass never runs. Pure wasted work, every step, for zero benefit. FIX (agent.py ~2245): removed the entire main-loop chokepoint block. Zero behavior change (proven dead, not guessed) -- the SWEEP's own separate verify-at-fill check (different call site, does a real immediate retry when a write doesn't stick, unaffected) stays. py_compile + pyflakes clean. NOT yet live-tested for the actual time saved. STILL PENDING from the audit: _verify_pass/_view_mismatches themselves (hundreds of lines) plus _verify_fix_count/_verify_dead_stable/_verify_clean_tabs/_verify_ran_once are all confirmed-dead but NOT YET DELETED -- that's a separate, bigger cleanup (same 'copy to scratch/ first, then remove' discipline as strip-where-crutches) deliberately not bundled into this speed-focused pass. Rest of the audit (which of the remaining ~15 state variables are KEEP/REPLACE/DROP for an eventual verb-loop) not yet done -- this was a targeted speed-lever stop within the audit, not the full audit." },
            { id: "sweep-deterministic-nav", owner: "Ralph", title: "Sweep's own navigation call short-circuited (speed fix)", status: "partial",
              note: "USER-FLAGGED 2026-07-14: 'no serious worker/org takes this seriously if it takes near an hour' — the ×10 run showed 14-21 min/record, traced to 59-121 LLM HTTP calls/record from _navigation_protocol (the sweep's navigation decision), NOT the fill-decision path (already ~2-3% LLM). Read that function's own prompt: it already builds the empty-field list deterministically and instructs the LLM to 'fill the FIRST one' with a value resolvable via the SAME record lookup bucket-1's OPT2 short-circuit already uses — the LLM was being paid 2-8s/call to echo a decision the function could already make. FIX: two short-circuits before any prompt/HTTP call — (1) try the section-aware record lookup on the first 5 empty fields, return the fill action directly if any resolves; (2) if the page is fully filled and a tab is unvisited, advance to it directly (no judgment needed). Falls through to the LLM unchanged for genuinely unresolvable fields — same design rule (LLM = ambiguity, not lookup). OFFLINE-VERIFIED (scratch/probe_sweep_deterministic.py, 4 cases). Syntax-checked; NOT yet live-retested — expect this to cut most of the 59-121 calls/record given the ×10 run's own logs show nearly every sweep fill was a clean record-driven value. Bucket 3 (verb-loop proper) is next if this isn't enough." },
            { id: "settled-blank-convergence", owner: "Ralph", title: "\"Keeps looping and looping\" on legitimately-blank tabs", status: "done",
              note: "USER-REPORTED LIVE 2026-07-14 while testing the speed fix: record 10 (claims-free) got stuck in repeated STUCK->jump->STUCK cycles on the Claims tab, 8 separate STUCK events, never converging. ROOT CAUSE: _navigation_protocol's empty-field-list builder had an 'attempted field counts as settled' guard scoped to CHECKBOXES ONLY (added 2026-07-09 for unchecked-box-reads-empty-forever). Text/combo fields correctly skipped as deterministic-absent-from-record hit the exact same shape (a skip never writes a value either) but weren't covered — stayed in the 'still needs a value' list FOREVER. FIX: broadened the guard from checkbox-only to any attempted field. OFFLINE-VERIFIED (scratch/probe_settled_blank_convergence.py). LIVE-CONFIRMED: next --start_record 10 run reached 100% completion, terminated naturally — the same record previously had to be manually killed at 24.4%. Convergence bug closed. This retest also caught a NEW crosstalk regression in the bucket-2 speed fix itself — see sweep-shortcircuit-section-confirm." },
            { id: "sweep-shortcircuit-section-confirm", owner: "Ralph", title: "Bucket-2 short-circuit regressed crosstalk on its first live test", status: "partial",
              note: "FOUND 2026-07-14: the same record-10 retest that confirmed the convergence fix also surfaced d2_dl_exp: expected '08/22/2029', got '04/11/2029' (Driver 2's own DL Expiration got Driver 3's value) — a fresh crosstalk instance from a call site that didn't exist until hours earlier. ROOT CAUSE: the bucket-2 sweep short-circuit calls _detect_section+_lookup_field directly but never checked the None-vs-'' distinction every OTHER section-aware call site was already hardened for (the _view_mismatches defer-on-None fix from earlier the same day) — reintroduced the exact bug class hours after it was closed elsewhere. FIX: added the same 'if section is None: skip, try next candidate' guard; trusts a confirmed '' exactly as before so unsectioned tabs (the common case) are unaffected. OFFLINE-VERIFIED (scratch/probe_sweep_shortcircuit_section_confirm.py, 3 cases). Syntax-checked; NOT yet live-retested. LESSON: every new section-aware lookup call site needs this guard as baseline, not optional hardening." },
            { id: "strip-where-crutches", owner: "Ralph", title: "Dead legacy auto-handler block removed (~716 lines); 3 live fallbacks deliberately left alone", status: "done",
              note: "AUDITED + ACTED ON 2026-07-14. Classified all 12 call sites for _focus_first_empty_field/_try_advance_tab by reading full context, not just grep. Result: much smaller problem than framed — 8 of 12 (all 4 _focus_first_empty_field sites + 4 of 6 _try_advance_tab sites) were PROVABLY DEAD CODE, gated behind _plugin_active = (task_plugin is not None) or pure_transformer or no_autohandlers, and run_task.py hardcodes all three such that _plugin_active is unconditionally True in every real run — that code has never executed. REMOVED: the entire dead block (~716 lines) deleted from agent.py per user's 'save to throwaway file first, then remove' instruction — exact copy at scratch/removed_dead_legacy_autohandlers_20260714.py. Verified safe (py_compile clean, LLMAgent imports clean, no orphaned references) — zero-behavior-change, no live test needed. Net -706 lines. REMAINING 3 sites (2 _try_advance_tab calls + 1 inline 'LLM stuck re-picking' block): investigated making them consult the model's own ranking instead of blind order — found they fire in a branch that runs BEFORE the transformer's prediction is even computed that step (t_pred = self._predict(state) happens later, in a separate branch these `continue` past). Fixing properly means restructuring WHEN the model gets consulted across the whole step loop — real architectural risk, not a local patch. USER DECISION 2026-07-14: leave these 3 alone — they only fire in genuine dead-ends (nothing clickable, scroll exhausted, LLM gave nothing usable), not common-case WHERE-steals, and the risk/reward of a core-loop restructure with zero live-test budget left didn't justify it. Revisit only if these specific fallbacks cause an observed problem." },
            { id: "orphaned-locals-from-strip", owner: "unassigned", title: "pyflakes: ~17 unused local vars left orphaned by strip-where-crutches", status: "pending",
              note: "Found 2026-07-15 running pyflakes (new standing check) over agent.py after the verify-convergence fix #1 edits. Lines ~183-2729: constants/counters (_NO_CHANGE_LIMIT, _DROUGHT_LIMIT, _TAB_STEP_LIMIT, _pane_escape_last_field, _pane_escape_streak, _tc_advance_verified, _MED_CONF, _last_auto_step, _TERMINAL_HINTS, plus a few unrelated pre-existing ones: active/bg/all_types/W/fl/_step_exc/_tab_cx) are assigned but never read anywhere in scope -- their only consumers were inside the ~716-line dead auto-handler block deleted for strip-where-crutches. No F821 (undefined-name) hits, so nothing is broken; this is pure dead-weight cleanup. DO: delete these orphaned assignments (verify each has zero remaining references first, same cross-reference-sweep discipline as the _focused_el casualty), rerun pyflakes clean. Not urgent -- zero behavior impact -- but leaves the file honest about what's actually live." },
            { id: "dead-code-removal-broke-focused-el", owner: "Ralph", title: "Dead-code deletion broke _focused_el (found + fixed same night)", status: "done",
              note: "The ~716-line dead-block removal swept away two UNCONDITIONAL lines (_focused_id/_focused_el computation, not gated by _plugin_active, sitting just before the dead if-block) that were still needed later at the pre-type clear check. First two post-removal runs crashed instantly (NameError, 0 steps). FIX: recomputed _focused_el fresh at its actual use site. Full sweep this time: cross-referenced all 94 names from the deleted block against agent.py -- only this one was a real casualty. LIVE-CONFIRMED: next run went 106 steps with no crash. LESSON: 'zero risk because dead' was true for gated logic but missed unconditional setup lines sharing the same delete range -- needs a full cross-reference sweep next time, not just a gating check." },
            { id: "verify-never-converges-hallucination", owner: "unassigned", title: "Verify never reaches 0 corrections -- dead-mark bookkeeping + LLM hallucination", status: "done",
              priority: 1,
              note: "FOUND 2026-07-14. FIX #1 (dead-mark bookkeeping) IMPLEMENTED 2026-07-15, not yet live-tested. ROOT SYMPTOM 1: fields ('Transmission', 'Color', 'Primary Use', 'Payment Frequency') get 'won't confirm after 2 tries -- accepting (dead)' on one pass, then get the SAME treatment again next pass. CAUSE CONFIRMED: dead-mark storage keyed by _attempt_key, which shares the same geometry-based section-detection logic as the model's training-time 'attempted' feature (transformer.py _attempt_key/_section_of -- deliberately never patched, would desync live inference from the trained checkpoint). That key can flip between passes on unsectioned tabs when a stale off-screen pane briefly contaminates the read, so a dead-mark silently fails to match on the next lookup. FIX: added self._verify_dead_stable (set, __init__ ~line 651) -- parallel (tab_name, bare_label.lower()) key, geometry-independent, can't flip. Wired into both _verify_pass branches (deterministic-fix branch ~line 5124, LLM-fallback branch ~line 5225): filters candidates against it, dead-marks into BOTH _dead_fill_keys and _verify_dead_stable when either counter exceeds 2, cleared on new-record reset (~line 2536). Confirmed by name that 'Claim Number' flagged on Vehicle AND Payment tabs was explained by the SAME root cause -- LLM-fallback branch's counter was already bare-label-stable but the STORED mark used the unstable geometry key. LIVE-CONFIRMED 2026-07-15 (run_20260715_150701.txt): verify converged 25->4->3->1->0 corrections across 5 passes, Submit fired autonomously (Submission: PAI-2026-00441_20260715_152324_642175.json). Dead-marks stuck: 'Years Continuously Ins'/'Years at Address' dead-marked once (15:20:18-20), never re-flagged in the 2 later passes; same for 'Collision Deductible' (15:21:54) and 'Garaging Location'/'Title State' (15:22:45) -- old symptom was re-flagging identical fields every fresh pass forever. FIX #2 (LLM hallucination) IMPLEMENTED 2026-07-15: root cause was _navigation_protocol's prompt telling the LLM 'pick a field ONLY from the EMPTY list' with nothing enforcing it. Added a validation block (agent.py ~line 5071): rejects any 'fill' action whose field isn't case-insensitively present in that call's own _empty candidate list, returning {\"action\":\"wait\"} instead. OFFLINE-DRILLED (scratch/probe_verify_hallucination_guard.py): 6/6 cases pass. The full run's own LLM-fallback branch never triggered (deterministic short-circuit resolved every field first), so LIVE-PROVED IT SEPARATELY 2026-07-15 via scratch/probe_navigation_protocol_hallucination_real.py -- calls the REAL agent._navigation_protocol (constructed LLMAgent, no live UI needed) with _cached_record forced empty (the only way to fall past the deterministic short-circuit into the actual LLM-fallback prompt path) and _llm_json monkeypatched to return the exact live hallucination ('Claim Number' on a fake Vehicle-tab state that doesn't contain it). Result: guard fired for real -- '[NAV] LLM proposed field 'Claim Number' not in this page's EMPTY list ['Transmission', 'Color'] -- rejecting as hallucination', returned {\"action\":\"wait\"}. Sanity check: an honest fill (real EMPTY field 'Transmission') still passed through unaffected -- no false positive. Both symptom-1 (full run) and symptom-2 (this drill) now live-proven via the real code paths, not logic replays. Status: DONE for this fix; the remaining bar is rerun-x10-post-fixes." },
            { id: "combobox-substring-match", owner: "unassigned", title: "Combobox fill picks the wrong option on first-letter collisions", status: "done",
              priority: 1,
              note: "FOUND + ROOT-CAUSED 2026-07-14. FIXED 2026-07-15, NOT YET LIVE-TESTED. BC mismatches: v_color expected 'Midnight Black' got 'Maroon'; v_usage expected 'Pleasure' got 'Personal/Pleasure'. ROOT CAUSE: the combobox fill ladder's first-letter-cycling rung (agent.py, now rung 4) cycled on the TARGET VALUE's own first letter only -- useless when the record's phrasing and the option's phrasing diverge in which WORD carries the match ('Midnight Black' doesn't share a first letter with the real option 'Black'; record 'Pleasure' is a substring of option 'Personal/Pleasure', not a prefix of it). FIX (agent.py ~6243, same rung the 2026-07-15 diagnostic-logging pass already touched for the Title State investigation): value is split into whitespace/slash/dash-separated tokens, tried longest-first (most specific = least collision-prone); EACH token seeds its own first-letter-cycling attempt; the match check now accepts substring containment BOTH directions (target-in-option OR option-in-target), not just equality/prefix. Fully generic, NO-HARDCODE-compliant -- every keystroke sequence is driven by the record's own text and the widget's own observed readback, no option lists baked in anywhere. Token-generation logic offline-verified: 'Midnight Black' -> ['Midnight Black','Midnight','Black'] (finds 'Black' via containment on the 3rd token); 'Pleasure' -> ['Pleasure'] (finds 'Personal/Pleasure' via containment on the 1st/only token). py_compile + pyflakes clean. LIVE-TESTED 2026-07-15 (run_20260715_185601.txt, --start_tab 2 drill): every Vehicle-tab combobox filled correctly on the first press -- 'Title State'->'california', 'Primary Use'->'commute', 'Garaging Location'->'private garage', 'Color'->'gray' (1 press each). CAVEAT: this record's Color value happened to be the exact string 'Gray', not 'Midnight Black' -- the substring-token path itself (multi-word divergent phrasing) wasn't actually exercised live, only the plain first-letter path was. USER ACCEPTED this result as sufficient evidence to move on (2026-07-15) -- marked done on that basis, not a live-confirmed substring-collision case. May also fully close verify-never-converges-hallucination's Symptom-1 fields that never actually got fixed correctly before (Transmission/Color/Primary Use/Payment Frequency) -- unconfirmed, would need a run against a record whose Color/Usage value actually diverges from the option text to prove it." },
            { id: "verify-llm-fallback-shortcircuit", owner: "unassigned", title: "Extend deterministic short-circuit into verify's own LLM-fallback branch", status: "done",
              note: "Task-list item #4 for 2026-07-15. FOUND CONCRETE EVIDENCE + FIXED 2026-07-15: analyzing the ×10 rerun's record 1 (run_20260715_195128.txt), the Claims tab fired 16 separate real LLM round-trips ([NAV] protocol calls) across just 3 verify passes, each resolving at most one field before the next pass rediscovered the same class of gap -- verify's OWN _needs_llm scan (agent.py, the LLM-fallback branch inside _verify_pass) stopped at the FIRST empty non-dead field and called the real LLM (_navigation_protocol) for it, even though THAT function's own opening short-circuit (deterministic value / confirmed-absent) would resolve most such fields without an LLM call anyway -- just never applied to verify's own gate-check first. Root problem: a confirmed-absent field (record present, genuinely nothing for it) had no dead-mark and no attempted-mark, so it sat there forever re-triggering a real LLM call every single pass. FIX: the _needs_llm scan (agent.py ~5297) now does the SAME section-aware deterministic lookup verify's branch-1 and the sweep's bucket-2 short-circuit already use, per candidate field, before ever deciding an LLM call is needed -- resolvable fields get filled directly (self._nav_fill_field, no LLM), confirmed-absent fields get self._mark_attempted'd and settled permanently (no LLM, and no longer re-flagged on future passes), only genuinely AMBIGUOUS fields (_detect_section returns None) still reach the real LLM. py_compile + pyflakes clean. LIVE-PROVED on the real code path (scratch/probe_verify_llm_fallback_shortcircuit_real.py -- constructs a real LLMAgent, calls the ACTUAL _verify_pass method, no live UI, _navigation_protocol monkeypatched to count calls instead of making one): 3-field fake view (one resolvable, one confirmed-absent, one ambiguous) -- resolvable field filled deterministically (no LLM), confirmed-absent field settled via mark_attempted (no LLM), ambiguous field correctly still reached the LLM fallback exactly once. All conditions pass. REGRESSION FOUND + FIXED SAME DAY (2026-07-16): user live-tested via a Claims-tab drill (run_20260716_014653.txt) and hit exactly the looping-forever complaint the fix was supposed to eliminate -- 'Years Continuously Insured' fired 8 IDENTICAL 'deterministic fix' log lines in under 30s (write not sticking, but this new path had ZERO retry cap, unlike branch-1's existing _verify_fix_count/_dead_fill_keys/_verify_dead_stable discipline), and 'Third Party Name'/'Third Party Policy' each 'settled' 5+ times in a row (the scan's dead/attempted exclusion check only tested _dead_fill_keys, never _attempted_keys, so mark_attempted's own bookkeeping was silently ignored on the next scan). FIX: added the missing `k in self._attempted_keys` check alongside the existing dead-keys check, and copied branch-1's exact 2-tries-then-dead-mark counter (_verify_fix_count + stable tab-label key) onto the deterministic-fill path. py_compile + pyflakes clean. RE-DRILLED on a targeted regression scenario (scratch/probe_verify_shortcircuit_no_infinite_repeat.py -- simulates a write that never sticks + a confirmed-absent field, across the REAL _verify_pass method's full 8-scan loop, not just one static state): fill attempts now cap at 2 (not 8), settle happens exactly once (not 6 times) -- both PASS. LESSON (self-inflicted, acknowledged directly to the user): shipped this fix straight to a live test without a repeat-scenario drill first -- the ORIGINAL drill only exercised a single static observation, never simulated the SAME field staying empty across multiple scan iterations, which is exactly where the missing retry-cap bug lived. Cheap-verify-first has to mean testing the ACTUAL failure shape, not just the happy path, or it gives false confidence. LIVE-CONFIRMED 2026-07-16 (run_20260716_025101.txt, full single-record run -- only_tab drills can't reach verify at all since only_tab guards _confirm_finished to always return False, so this needed a real run): Claims tab fixed 4 real mismatches pass 1, then '[VERIFY] Claims: 0 mismatches, no LLM need -- marking clean, skip on next pass' in pass 2 -- ZERO LLM calls wasted on Claims after that, vs the original 16 round-trips across 3 passes. Whole run converged 23->2->3->0, submitted, 10.2 min -- fastest yet (down from 16.4 baseline). No repeat-spam regression. Both the original fix and the regression fix are now proven on the real form, not just drills." },
            { id: "only-tab-lock-flag", owner: "unassigned", title: "'--start_tab' drills wander off the target tab -- built a real lock", status: "done",
              note: "User hit this THREE times total. FIRST ATTEMPT (2026-07-16): patched the main-loop tab-click-detection site with a snap-back-to-locked-tab override + guarded _confirm_finished. LIVE-TESTED, STILL FAILED -- 'navigated away again' -- because the SWEEP has its OWN separate tab-advance code path ('[NAV] tab swept clean -> switch tab') that executes a click directly, never going anywhere near the patched site. SECOND ATTEMPT: patched that specific sweep site too -- but a grep afterward found 5+ total places that can click a tab (prediction-driven, sweep x2, fixation-recovery, GAP-driven, _try_advance_tab), meaning chasing one at a time as the user hit each live was never going to converge. REAL FIX (2026-07-16, mirrors this project's own Submit-chokepoint pattern exactly): reverted both point-patches, wrapped self._executor.execute ONCE (agent.py ~911, right next to the existing Submit guard) -- every one of the 44 call sites that route through self._executor.execute, regardless of which internal function calls it, now gets checked against self._other_tab_bboxes (all tab-strip bboxes except the locked one, accumulated across steps same as _submit_bboxes) via the new _point_on_other_tab helper (~5891, mirrors _point_on_submit exactly) and blocked if it lands on a different tab. LIVE-TESTED (run_20260716_021901.txt): lock WORKED -- zero 'Tab click detected' lines, 7 separate navigation attempts (Policy/Policyholder/Vehicle/Coverage/Drivers/History/Payment) all correctly BLOCKED, the run never actually left Claims. NEW GAP FOUND SAME TEST: once the locked tab was genuinely done, the agent had nowhere else to go and tried every other tab in turn, one per step, each blocked -- 7 wasted ~3-4s cycles (~22s of pure spin) before the user interrupted, exactly the 'so much wasted steps' complaint. FIX: self._only_tab_blocked_streak counter (agent.py ~903) increments on each blocked navigation click, resets on any real action; the main step loop (~939) now breaks cleanly once the streak hits 3, logging '[ONLY-TAB] locked tab has nothing left to do' instead of spinning until max_steps or a manual interrupt. py_compile + pyflakes clean on both files. LIVE-CONFIRMED 2026-07-16 (run_20260716_022508.txt): 3 blocked navigation attempts (Policy/Policyholder/Vehicle), then '[ONLY-TAB] locked tab has nothing left to do (3 consecutive blocked navigation attempts) -- stopping drill' fired and the run ended CLEANLY on its own (17 steps, 'Run ended' -- no manual interrupt needed this time). Zero 'Tab click detected' lines -- never actually left Claims. Both the lock and the stop-when-done fix are proven live. User confirmation: 'it actually fucking worked, nice.'" },
            { id: "combobox-write-readback-desync", owner: "unassigned", title: "'Title State'/'Garaging Location' combobox fix reported success but next verify pass still sees it wrong", status: "done",
              priority: 1,
              note: "FOUND 2026-07-15 (run_20260715_161121.txt, live-watched, user called it an infinite loop -- corrected to bounded-but-wrong: 22->11->2->2->1, dead-marked and one pass from hitting 0 when interrupted; user's response 'in real workflows, it can't be doing that' correctly rejected bounded-but-wrong as still unacceptable). FIRST HYPOTHESIS (read/write identity desync, same shape as the 2026-07-14 checkbox bug) IMPLEMENTED as self._live_field_value + wired into _view_mismatches/_field_matches, but LIVE-TESTED 2026-07-15 (run_20260715_173009.txt) and HAD NO EFFECT -- 'Title State' still got fixed twice (17:40:53, 17:41:59) before dead-marking, identical to before. Hypothesis was WRONG (or at best incomplete) -- superseded by direct live diagnosis below. REAL ROOT CAUSE (confirmed via scratch/probe_setvalue_echo_or_real.py, attached to the live open form): the combobox WRITE ladder (_act_on_element, agent.py ~6147) has THREE of its four rungs broken for this control class (a 50-state-style wx.Choice dropdown). Rung 0 (direct ValuePattern.SetValue) THROWS a COM error every time -- confirmed live. Rung 1 (expand+select-by-name) is a no-op -- this control doesn't support ExpandCollapsePattern at all (GetExpandCollapsePattern() returns None). Rung 2 (native type-to-filter: SendKeys(full_word)+Enter) is NOT prefix matching on this widget -- it's single-KEYSTROKE type-ahead, so typing 'California' only responds to the LAST character sent and lands on 'Alabama' (first state starting with trailing 'a') -- confirmed live, reproduced exactly. Only rung 3 (first-letter cycling, press 'C' repeatedly) actually works, and when tested in isolation it succeeds AND persists through a tab-switch round-trip (Vehicle->Policy->Vehicle), meaning the write mechanism ITSELF is not inherently unreliable. FIX APPLIED: skip rung 2 entirely for multi-character values (agent.py ~6218) since it can never legitimately succeed on this widget class and was previously perturbing the control's position before rung 3 even started; added diagnostic logging ('[COMBO] ... rung-4 first-letter-cycling hit ... after N press(es)' and an 'ALL write rungs failed' warning) so the exact rung and read-back value are visible on the next live run. py_compile + pyflakes clean. STILL OPEN: WHY the live run sees repeated re-fixes when the isolated probe shows rung 3 reliably works and persists. NARROWED 2026-07-15 (run_20260715_181231.txt): the diagnostic logging shows the write reports success ('california', 1 press) at 18:23:05, then Vehicle isn't revisited again until 18:24:14 -- FOUR intervening tab switches (Coverage/Claims/Payment/Policyholder) and ~70s -- and it's wrong again at 18:24:19. The isolated probe only tested ONE tab round-trip and it held; the real run does several. Since the write mechanism itself is proven solid (native Win32 combobox keypress, not a UIA echo), the leading suspect is now STALE-GEOMETRY MISRESOLUTION: _resolve_live_control disambiguates same-named duplicate nodes by nearest bbox-center distance -- if the observed elem's bbox is stale from before an intervening scroll/tab-switch shift, the 'nearest' live match could resolve to a DIFFERENT physical node than the one written to (this file already documents wx leaving hidden tab pages' duplicate controls in the tree as the root of a near-identical bug for checkboxes, 2026-07-14). NOT YET CONFIRMED -- next diagnostic step: log which live node _resolve_live_control actually picks (its bbox at call time) both at write time and at the later failed read, to prove/disprove wrong-node resolution directly instead of inferring from timing alone. PRACTICAL IMPACT NOTE: two consecutive live runs since this file's rung-2 fix (run_20260715_181231.txt: 28->2->4->0, 13.7min, submitted; run_20260715_183114.txt: 21->2->2->0, 12.6min, submitted) both converged and submitted cleanly despite this bug still occurring -- the dead-mark termination guarantee bounds it and it no longer blocks completion, but the underlying flakiness is real and unexplained, not something to consider closed. self._live_field_value is kept (still more correct than trusting a stale snapshot in general) even though it wasn't the fix for this specific bug.\n\nCLOSED 2026-07-16, USER CALL, NOT REPRODUCING: checked the latest run (run_20260716_133531.txt) for the three previously-flagged fields ('Title State', 'Garaging Location', 'Collision Deductible') -- all three filled correctly on the FIRST attempt this run, single press, zero retries, zero repeat-fix activity. Whole-run 'Top mismatches' list was EMPTY (100% value accuracy across all 113 filled fields). No current live evidence this bug is still occurring. Caveat noted before closing: _verify_pass no longer runs at all (per the 2026-07-16 'no revisit' change), so the ORIGINAL discovery mechanism for this bug (verify re-fixing the same field across multiple passes) can no longer fire even if the underlying write flakiness still exists somewhere -- it would now just silently ship wrong once with no visibility, rather than get caught and logged. User explicitly chose to close rather than chase a non-reproducing bug -- reopen if the symptom (a field 'succeeding' then reading wrong later) resurfaces in a future run.\n\nREOPENED 2026-07-16, SAME DAY: found real evidence it's still happening, just no longer VISIBLE the same way. Investigating a user-reported 'pauses at a certain point' speed complaint (via new temporary observe()-count instrumentation), found step 101 of run_20260716_141849.txt made 41 observe() calls in ~29 seconds -- turned out to be the SWEEP filling ~20 Coverage-tab fields in one un-tallied step. Of those ~20 fields, 9 needed a 'verify-at-fill retry ... didn't stick first try' -- the SAME write-flakiness this bug was originally about. It didn't go away; the 2026-07-15 verify-at-fill fix just catches and silently retries it WITHIN the same step now, instead of leaking through to a later verify pass where it used to get discovered and logged as a mismatch. Cost: ~9 extra observe+refill cycles in one step alone, a real and current chunk of wall-clock time. AND: one retry still failed even after the extra attempt -- 'Collision Deductible' expected '500', landed on '5000' (BC scorecard: cov_collision_ded mismatch) -- see combobox-numeric-substring-collision below for that specific bug and its fix. Net: the ORIGINAL write-flakiness (why does the first attempt often not stick) is still unexplained and still costing real time on every run; only its visibility changed. Status set back to partial, not done.\n\nNEW HYPOTHESIS 2026-07-16 (run_20260716_144654.txt): 100% correlation found -- checked WHICH fields retry vs don't in this sweep: 'At-Fault Accidents'->'0', 'Not-At-Fault Accidents'->'0', 'Total Accidents'->'0', 'Moving Violations'->'0', 'Comprehensive Claims'->'1', 'Total Claims Filed'->'1' ALL retried; 'DUI/DWI on Record'->'NO', 'SR-22/FR-44 Filed'->'NO', 'License Suspended'->'NO' in the SAME sweep never retried. Every retry is a short numeric value (likely SpinCtrl widgets); every non-retry is YES/NO or text. Not random flakiness -- a specific widget-type pattern. HYPOTHESIS: the write genuinely succeeds, but the sweep's OWN verify-at-fill read-back (self._observe() called with ZERO settle delay immediately after self._nav_fill_field returns) races wx's UI update for SpinCtrl-style widgets specifically, reading a stale value before the widget visually commits -- the retry attempt naturally has more elapsed time before ITS OWN check and succeeds, fitting a timing race better than genuine write unreliability. CHEAP TEST APPLIED (agent.py ~6225, the sweep's verify-at-fill site): added time.sleep(0.15) between the fill call and the read-back observe(), mirroring the settle sleeps the write rungs themselves already use after SetValue/RangeValue calls. NOT YET LIVE-TESTED -- this is a hypothesis test, not a confirmed fix. If the next live run's retry count for these specific numeric fields drops to near-zero, the timing-race theory is confirmed; if retries persist at the same rate, the write itself is genuinely unreliable and this delay was the wrong lever.\n\nHYPOTHESIS PARTIALLY CONFIRMED THEN CONTRADICTED (run_20260716_150010.txt then run_20260716_152340.txt): the 0.15s delay dropped retries 9->3 on the first follow-up run (supported the SpinCtrl timing-race theory) -- but the VERY NEXT run showed retries back up to 8, and critically ALL 8 were Coverage-tab COMBOBOXES (Bodily Injury, Property Damage, Collision Deductible, Comprehensive Deductible, UM/UIM Limit, Rental Limit, Total Premium, Payment Frequency), not the numeric SpinCtrl fields from before -- contradicts a clean 'widget type = SpinCtrl' explanation. REAL ROOT CAUSE FOUND: the sweep call site (agent.py ~6183) was DISCARDING _nav_fill_field's own return value, then independently re-verifying via a FRESH self._observe() full-UIA-snapshot re-lookup by label + _field_matches -- but the combobox rung-4 cycling ladder (_act_on_element, agent.py ~6624) already reads back the value off the SAME live ctrl object right after writing it and only returns True on a CONFIRMED match. The sweep's second, independent snapshot-based check was redundant and, for comboboxes specifically, prone to seeing a stale a11y-tree value (selection-changed events lag the snapshot) -- flagging already-correct fields as 'didn't stick'. FIX (agent.py ~6183-6220): capture _nav_fill_field's return as _fill_confirmed; when True, trust it and skip the redundant re-observe+re-check entirely (mark verified, refresh state, move on) -- only fall through to the settle-delay + re-check + retry-once path when the write function itself couldn't confirm. py_compile + pyflakes clean (ast.parse OK, zero F821). Unifies both runs' observations: SpinCtrl (RangeValuePattern) and combobox (rung-4) writes both already self-verify on write; the redundant snapshot re-check was the actual source of false retries in both cases, not a real write flakiness. NOT yet live-tested -- next run should show retries drop toward zero for confirmed-write cases, with any REMAINING retries now being a genuine signal (the write path itself couldn't confirm), not a false snapshot-lag positive.\n\nLIVE-CONFIRMED 2026-07-16 (run_20260716_163430.txt, first uninterrupted run since the fix + the checkbox-verb unification): ZERO 'verify-at-fill retry' lines anywhere in the whole run -- the sweep-re-verify fix worked exactly as intended, no false 'didn't stick' flags on Coverage-tab comboboxes or anywhere else. Value Accuracy 100% (59/59 typed values correct), Tab Coverage 100% (all 11 sections including Discounts -- the bc-fidelity-discounts-tab-detection scorer gap also resolved itself, no scorer changes needed), zero errors/tracebacks, natural termination via deterministic Submit, 8.5min duration (normal range). Checkbox verb unification also confirmed correct on every checkbox this run (record-NO left unchecked, record-YES checked via the canonical verb, no force-check errors). REMAINING GAP (separate, pre-existing, not caused by today's fixes): Field Match Rate 65.6% (107/163) -- fields the agent left blank that should have had a value; Value Accuracy of what WAS filled is 100%, so this is a coverage gap (fields never reached/attempted), not a correctness regression. Status set to done -- the write-flakiness/false-retry problem this node tracked is resolved; the separate field-match-rate coverage gap is its own open question, not filed here." },
            { id: "combobox-numeric-substring-collision", owner: "unassigned", title: "Substring-match fix falsely matches numeric values whose digits are a PREFIX of another option ('500' inside '5000')", status: "done",
              priority: 1,
              note: "FOUND 2026-07-16 (run_20260716_141849.txt): BC scorecard showed cov_collision_ded expected '500', got '5000' -- traced to the exact fill attempt in the log: '[COMBO] Collision Deductible: rung-4 cycling (token 500) hit 5000 after 1 press(es) of 5'. ROOT CAUSE: the multi-token substring-match fix (2026-07-15, built for TEXT like 'Pleasure' inside 'Personal/Pleasure') checks containment both directions -- but for NUMBERS this produces false positives: '500' IS literally a substring of '5000' (same leading digits), a digit-sequence collision, not a real semantic match. The form's actual Collision Deductible options (car_insurance_form_wx.py) are ['0','100','250','500','1000','2000','2500','5000'] -- pressing '5' cycles between the two options starting with '5' ('500' and '5000'), and the loose substring check accepted whichever one it landed on first instead of requiring the real one. FIX (agent.py ~6602, same rung-4 cycling match block the apostrophe fix touched): added a numeric-exact-only guard -- if the target value (after stripping $/,/. decoration) is purely digits, ONLY an exact match counts; the substring-containment checks are skipped entirely for numeric targets, forcing the cycle to actually land on the correct number instead of accepting a same-prefix decoy. Text values (the ORIGINAL reason the substring check exists) are unaffected -- still get the full containment + apostrophe-insensitive matching. py_compile + pyflakes clean. Logic-drilled (4 cases): '5000' vs target '500' no longer falsely matches; exact '500' vs '500' still matches; the Pleasure text-substring case still works; the Associate's-Degree apostrophe case still works. All PASS. NOT yet live-tested against the real form." },
            { id: "verify-skip-clean-tabs", owner: "unassigned", title: "_verify_pass re-scans ALL 8 tabs every pass, even ones already clean", status: "done",
              priority: 2,
              note: "FOUND + IMPLEMENTED 2026-07-15. FIX: new self._verify_clean_tabs set (agent.py __init__ ~line 663) -- a tab that finishes its scan-loop with zero _mm hits AND no LLM-fallback trigger (_tab_had_activity stays False) gets added to it; the outer tab loop (~line 5118) skips (continue) any tab already in the set instead of clicking into it. Safe because fills are pinned to their own tab via prefer_key (no cross-tab writes possible) and dead-marks are permanent+global via _verify_dead_stable regardless of re-scan. Cleared on new-record reset alongside _verify_dead_stable. py_compile + pyflakes clean. LIVE-CONFIRMED 2026-07-15 (run_20260715_161121.txt): repeated '0 mismatches, no LLM need -- marking clean, skip on next pass' log lines fired correctly (Policyholder/Coverage/Drivers/Policy etc.), and skipped tabs were not re-clicked on later passes. Working as designed." },
            { id: "step-delay-cut", owner: "unassigned", title: "STEP_DELAY 1.5s -> 0.7s -- dominant per-record wall-clock cost was idle sleep, not LLM", status: "done",
              priority: 1,
              note: "FOUND + CUT 2026-07-15. Investigated 'why does one record take 16.4 min' -- assumption going in was LLM latency; log evidence disproved it. run_20260715_150701.txt: main-fill phase = 10.4 min with only ~15 LLM calls in it (most sub-second); step-by-step timestamps showed ~2-5s between EVERY step regardless of whether an LLM fired. Root cause: STEP_DELAY=1.5 (run_task.py:143), a fixed time.sleep applied at 80+ call sites after nearly every action over 150-200+ steps/record. FIX: cut to 0.7s. Zero model-perturbation risk (wall-clock pause, not an injected action). LIVE-TESTED 2026-07-15 (run_20260715_161121.txt): Wasted Step Rate 9.5% (10 no_change / 105 actionable) -- comparable to baseline, no obvious no_change/validation-failure spike from the tighter delay. NOT a full duration comparison, though -- this run was interrupted by the user at step 105 (mistaken for an infinite loop, see combobox-write-readback-desync for what was actually happening) before it could reach Submit, so the new duration metric never printed a comparable end-to-end number. NEXT: one clean uninterrupted run to get an actual before/after duration reading, now that combobox-write-readback-desync is also fixed and the run should converge without the double-fix noise that made this one look stuck." },
            { id: "bc-duration-metric", owner: "unassigned", title: "BC scorecard now reports wall-clock duration (start-to-submit)", status: "done",
              note: "ADDED 2026-07-15 per direct request, alongside the speed investigation above -- there was no way to see run duration trend over time without manually diffing log timestamps. scripts/bc_fidelity.py score_run(): duration_seconds = submission_file.mtime - run_start_ts (run_start_ts was ALREADY threaded through for the stale-submission guard, so this needed zero new instrumentation). Printed in the scorecard ('Duration X.X min'), written to the progress log entry, and shown as a new Dur(min) column in --progress's trend table. None-safe for older callers that don't pass run_start_ts. py_compile + pyflakes clean. Populates automatically starting the next live run through run_task.py." },
            { id: "rerun-x10-post-fixes", owner: "unassigned", title: "Rerun the full ×10 pass to validate everything built since the last one", status: "done",
              deps: ["verify-never-converges-hallucination", "combobox-substring-match"],
              note: "The earlier ×10 pass (see run-x5, correctly marked done) validated the driver-crosstalk fix chain across 9 records -- that result stands and isn't being re-litigated. But a lot of NEW code has landed since that run and has never been tested at ×10 scale: checkbox read/write identity fix, verify absent-field guard, scroll-to-top crosstalk fix, the 716-line dead-code removal (+ the _focused_el regression it caused, fixed), and once done, verify-never-converges-hallucination + combobox-substring-match. This is the first real test of ALL of that together, not a repeat of the earlier proof. Bar to clear: same as before -- all (or effectively all) records complete cleanly, natural termination, zero crosstalk mismatches.\n\nRUN 2026-07-15 (run_20260715_195128.txt, 19:51-22:15, ~2h24m wall-clock): 10/10 records SUBMITTED, all natural termination (verify converged to 0 on every record, no crashes, no manual interrupts), value accuracy 97-100% on every record. ZERO crosstalk-signature mismatches (no Driver 2/3 label-collision pattern in any of the 10 top-mismatches lists) -- BAR CLEARED. Residual mismatches found are already-tracked, SEPARATE problems, not crosstalk: (1) cov_collision_ded wrong on 3 records -- same combobox-write-readback-desync flakiness, still open; (2) v_color/v_usage/v_transmission 'mismatches' are actually the substring-match fix WORKING CORRECTLY (picked the best real option, e.g. 'Black' for 'Midnight Black') -- the scorer grades against literal intake text with no tolerance for 'closest available option', not an agent bug; (3) NEW, unexplained: 'ph_education: expected Associate Degree, got Some College' (record 5) -- not a first-letter collision (different starting letters entirely), not yet investigated, filed as a fresh open question. SPEED FINDING (the reason this took so long to run): per-record duration 12.2-20.3 min (avg 14.2), record 5 a 20.3min outlier -- total 2h24m for one ×10 validation pass is itself the strongest argument yet for pushing speed further; see step-delay-cut/verify-skip-clean-tabs notes above for what's already landed and why sub-1-min needs an architecture change (batch fills instead of observe-per-step), not another delay tweak." },
            { id: "ph-education-mismatch", owner: "unassigned", title: "'Education Level' wrong value -- apostrophe mismatch broke the combobox substring-match, not a first-letter collision", status: "done",
              note: "FOUND 2026-07-15 in the ×10 rerun (run_20260715_195128.txt, record 5): ph_education expected 'Associate Degree', got 'Some College'. Doesn't fit the already-understood bug shapes -- not a first-letter-cycling collision (Associate vs Some start with different letters), not a crosstalk pattern (Education Level is Policyholder-tab, non-repeated-section). ROOT CAUSE FOUND 2026-07-16 (read the actual log around record 5, line 11271): '[COMBO] Education Level: ALL write rungs failed for value Associate Degree -- falling through to legacy click/paste path' -- the fallback landed on 'Some College' (likely whatever the combobox already held, since the legacy paste path silently no-ops on a non-editable combo). Checked the form source (car_insurance_entry/car_insurance_form_wx.py:53-55): the real dropdown option is \"Associate's Degree\" (WITH the possessive apostrophe) -- the intake record's value 'Associate Degree' has NO apostrophe. The multi-token substring-match fix (2026-07-15) already cycles to the right option ('Associate's Degree' IS reached by pressing 'A'), but the match-check compares against the RAW value/option strings -- 'associate degree' is NOT a substring of \"associate's degree\" because the possessive 's sits right between 'associate' and 'degree', breaking direct containment. Every OTHER education value in the intake data happens to already include the apostrophe in both the record text and the form option, so this only ever surfaced for this one specific case -- genuinely a general punctuation-sensitivity gap, not something specific to 'Associate'. FIX (agent.py ~6596, the rung-4 cycling match check): added an apostrophe-insensitive comparison alongside the existing exact/substring checks -- strips \"'s\"/'’s' (the possessive suffix as a unit, not just the bare apostrophe -- stripping only the apostrophe character left a stray 's' that broke the match on the first attempt, caught by the drill before shipping) then any remaining bare apostrophes, on BOTH sides, before the containment check. py_compile + pyflakes clean. Logic-drilled (4 cases): the exact live case ('associate degree' vs \"associate's degree\") now matches; exact-match cases (\"bachelor's degree\") still work; the prior substring fix (Pleasure/Personal-Pleasure) still works; unrelated values still correctly rejected. All PASS. NOT yet live-tested against the real form (this record's specific value doesn't come up on record 1, the default test record -- would need --start_record 5 or similar to re-exercise it live)." },
            { id: "merge-click-override-discards-value", owner: "unassigned", title: "_merge's transformer-click-override discarded a resolved value for ANY fillable, not just comboboxes -- 3m38s / 166-repeat fixation on 'Accidents (3 yr)'", status: "done",
              priority: 1,
              note: "FOUND + FIXED 2026-07-16, live-caught during a --start_tab 6 (no lock) Drivers-tab drift (run_20260716_022849.txt) -- user: 'what the fuck was that loop', rightly alarmed since this looked like a generalization failure but wasn't. ROOT CAUSE: two separate parts of the code disagree about type-vs-click, and the wrong one won. Option B (agent.py ~1338, documented, intentional) decides fill-vs-navigate purely from the FOCUSED WIDGET'S TYPE, specifically BECAUSE the transformer's own action-type head is known-unreliable (whipsaws click<->keyboard across retrains, per the adjacent comment) -- correctly identified 'Accidents (3 yr)' as a fillable empty spin control needing TYPE, resolved its real value '0' with zero LLM calls. But _merge (agent.py ~4627) has ITS OWN, separate override: 'if the transformer says click with 92%+ confidence, trust it over the LLM's type decision' -- built for comboboxes (which genuinely need a click before you can select an option) but applied UNCONDITIONALLY to every focused fillable. The transformer's action-type head said 'click' at conf=0.94 for this plain SpinCtrl field, _merge's override fired, discarded the resolved '0' entirely, and returned a bare click with NO text at all -- a click can never enter a value into an edit/spin control, so the field stayed empty forever, and the model kept re-anchoring the optimal-viewport-jump on the same still-empty field: 166 occurrences of 'Accidents (3 yr)' across 3m38s continuous, the fixation-escalation safety net never firing once (0 'FIXATED' log lines in the whole run). '0' being a falsy-LOOKING but genuinely valid answer (zero accidents) made this especially bad, but the underlying bug hits ANY value on ANY non-combobox fillable the transformer is 92%+ confident about clicking -- not zero-specific. FIX (agent.py ~4627): added a focused-element-type check inside _merge itself (_foc_is_combo, looked up the same way the OPT2 caller already does) -- the transformer-click-override now ONLY fires when the focused field is actually a comboboxcontrol, restoring the override to the one case it was designed for and letting edit/spin/checkbox fields keep their resolved type+value. py_compile + pyflakes clean. LIVE-PROVED on the real _merge method (no live UI, calls the actual function twice): Case 1 (plain SpinCtrl field, transformer says click conf=0.94, LLM resolved '0') now correctly returns a TYPE action with text='0' preserved -- the exact live bug, fixed. Case 2 (combobox field, same transformer confidence) still correctly overrides to a click -- the original, intended behavior for comboboxes is unchanged. Both PASS. LIVE-CONFIRMED 2026-07-16 (run_20260716_024515.txt, --start_tab 4 --only_tab Drivers-tab drill): 'Violations (3 yr)' -> '0' typed correctly, 'Accidents (3 yr)' -> '0' selected correctly, another driver's 'Violations (3 yr)' -> '1' also correct -- zero fixation, zero repeat spam, the exact bug from run_20260716_022849.txt is gone. Lock + auto-stop also held (3 blocked attempts, clean stop at step 14).\n\nFOLLOW-ON BUG FOUND SAME DAY (run_20260716_124336.txt), while investigating the missing 'Discounts' section for a Navigation Protocol question: 'Collision Deductible'/'Comprehensive Deductible'/'Bodily Injury (k$/k$)'/'Property Damage ($)' all abandoned as 'leave-blank/empty -- Tab past (skip)' in a row on the Coverage tab, DESPITE having real resolved values ('500'/'250'/'100/300'/'100,000') -- this Coverage-tab abandonment (not a scroll-reach failure) is very likely what actually caused the GAP-forced tab-advance off Coverage before Discounts was ever reached. ROOT CAUSE: restricting the merge override to comboboxes (the fix above) was correct, but the DOWNSTREAM OPT2 leave-blank guard (agent.py ~1396) wasn't updated to match -- it reads ANY merged prediction with no 'text' field as 'the value resolved to blank', but a combobox-override click legitimately has no text field at all (a click is step one of opening/selecting a combobox, not a decision that the value is blank). Every legitimate combobox click-override was being misread as a blank-skip, abandoning a real value. FIX (agent.py ~1396): the leave-blank check now also requires prediction.get('action_type') != 'click' -- a click is never treated as blank regardless of missing text; only genuine type/keyboard actions with empty/blank-marker text still skip, exactly as originally designed. Logic-verified (4 cases: click-with-no-text passes through, genuine-empty-type-text still skips, real-value-type-text passes through, explicit '(leave blank)' marker still skips) -- all PASS. The full method is deep in the 400+-line main step loop, not unit-testable in isolation, so this is a condition-level drill, not a full-method one.\n\nLIVE-CONFIRMED 2026-07-16 (run_20260716_131631.txt): the fix worked exactly as designed -- NONE of the previously-broken fields (Collision Deductible/Comprehensive Deductible/Bodily Injury/Property Damage) got falsely abandoned this run, every leave-blank entry now looks like a genuinely absent record value (Suffix, PIP Limit, Third Party Name/Policy, Bank fields for a non-bank-payment record, etc.), and no more '[GAP] LLM stuck re-picking -- forcing unvisited tab' force-abandonment on Coverage either. BUT: Tab Coverage stayed at 90.9% (Discounts STILL missing) and Field Match Rate barely moved (61.3% vs 62.0% before) -- proves the ORIGINAL HYPOTHESIS WRONG: this merge/leave-blank bug and the missing-Discounts gap are two separate, unrelated problems, not cause-and-effect. What's actually happening: the agent visits Coverage, fills everything VISIBLE, and legitimately decides 'tab done' without ever scrolling far enough to discover Discounts exists below the fold -- a genuine scroll/reveal gap in Navigation Protocol (see navigation-protocol-discounts-scroll-gap node), separate from this fix. This specific fix (merge-click-override + leave-blank guard) is DONE and confirmed; the Discounts gap needs its own investigation." },
            { id: "navigation-protocol-discounts-scroll-gap", owner: "unassigned", title: "Checkbox click handler force-checked EVERY clicked box regardless of the record -- not a scroll gap, a missing record-lookup", status: "done",
              priority: 1,
              note: "FOUND 2026-07-16. ORIGINAL HYPOTHESIS WAS WRONG, RETRACTED: first suspected a scroll-to-bottom gap (this tab-'done' fires before reaching the below-fold Discounts section). Checked live (run_20260716_131631.txt) via the actual log, not just the aggregate metric: Discounts WAS visited and worked on this run -- 'Multi-Car', 'Good Driver (5+ yr clean)', 'Multi-Policy / Bundle', 'Good Student' all got BM_SETCHECK activity. So it's not a scroll/reveal gap at all. REAL FINDING (cross-checked against data_entry_tasks/data_entry_intake.txt, record 1's actual Discounts values): 'Multi-Car' (record: NO) got WRONGLY checked, 'Good Student' (record: NO) got WRONGLY checked, 'Loyalty Discount' (record: YES) never got touched at all -- only 'Good Driver'/'Multi-Policy' (both record: YES) came out correct, and only by coincidence. ROOT CAUSE FOUND (agent.py ~1824, the main per-step 'click landed on a checkbox' handler): this path unconditionally forced EVERY clicked checkbox to CHECKED via BM_SETCHECK, never consulting the record at all -- it only guarded against re-clicking an ALREADY-checked box (double-toggle), never against checking a box the record says should stay NO. This is a DIFFERENT, less careful implementation than the sweep's own checkbox path (_act_on_element), which already does the record lookup correctly -- this per-step path just never had it. Explains the Discounts wrong-checks exactly, and likely explains a much bigger slice of the overall Field Match Rate gap (62%) since checkboxes are everywhere on this form (Vehicle safety features, Coverage's Additional Coverages, etc.), not just Discounts -- any checkbox the transformer's pointer ever lands on outside the sweep's own path was getting force-checked regardless of the correct answer. FIX (agent.py ~1824): added a record lookup (_lookup_field/_detect_section, same pattern used everywhere else) BEFORE deciding to check a box -- only checks ON when the record actually says yes/checked/true/1; a record value of NO/absent now leaves the checkbox at its unchecked default, matching the sweep's own already-correct behavior. py_compile + pyflakes clean. Condition-level logic drilled (3 cases: record NO -> no check, record YES -> checks, record absent -> no check) -- all PASS; the surrounding code is deep in the main step loop, not unit-testable as a whole. STILL SEPARATE AND OPEN (at time of writing): 'Loyalty Discount' never got navigated to at all in run_20260716_131631.txt -- the OTHER open question from the same session ('transformer misses available empty fields on screen').\n\nLIVE-CONFIRMED 2026-07-16 (run_20260716_133531.txt): fix works exactly as designed. 'Multi-Car' (NO) and 'Good Student' (NO) now correctly left unchecked; 'Good Driver'/'Multi-Policy'/'Loyalty Discount' (all YES) all correctly checked THIS TIME, including Loyalty Discount which was missed entirely last run (transformer navigated to it fine this run -- may have been model-order variance, not a separate bug; keep an eye on it but not chasing further right now). Field Match Rate jumped 61.3% -> 69.3% (100->113/163), BC Score 47.6% -> 49.7% -- confirms the checkbox bug really was a large chunk of the overall gap, not just Discounts-specific. Duration 9.9min (vs 7.9min best case) -- within normal run-to-run variance, not a regression. ONE ODD LEFTOVER: Tab Coverage still reports 90.9%/Discounts still 'missing' in the aggregate metric, DESPITE every Discounts checkbox now confirmed correct in the log -- strongly suggests scripts/bc_fidelity.py's own tab/section-detection has a bug recognizing 'Discounts' as a counted section, separate from agent behavior entirely. Filed as bc-fidelity-discounts-tab-detection below, low priority, not blocking." },
            { id: "bc-fidelity-discounts-tab-detection", owner: "unassigned", title: "BC scorer's Tab Coverage never counts 'Discounts' even when every field in it is confirmed correct -- likely a scorer bug, not an agent bug", status: "done",
              note: "FOUND 2026-07-16 (run_20260716_133531.txt): all 8 Discounts checkboxes confirmed correct in the agent log (Multi-Car/Good Student correctly left unchecked, Good Driver/Multi-Policy/Loyalty Discount correctly checked) -- yet Tab Coverage still reports 90.9% with 'Discounts' absent from the covered-sections list, same as every prior run regardless of actual correctness. Since the underlying field-level behavior is now proven correct, this points at scripts/bc_fidelity.py's own section/tab classification logic (whatever builds the covered-tabs list for the metric) not recognizing 'Discounts' as a valid/countable section at all -- a scorer-side gap, not an agent behavior gap. DO: read bc_fidelity.py's tab-coverage computation, check what determines section membership for the 'Discounts Applied' subsection specifically (likely a prefix-matching or section-name mapping that doesn't include 'disc_' fields or the 'Discounts' section label). Low priority -- doesn't affect real agent behavior, only a reported metric's accuracy.\n\nRESOLVED-BY-OBSERVATION 2026-07-16 (run_20260716_163430.txt): Tab Coverage reported 100% this run, 'Discounts' present in the covered-sections list alongside all 10 other tabs -- checked scripts/bc_fidelity.py's working tree for any change to the tab-coverage/section-detection logic since the bug was filed (git diff) and found NONE -- the scorer code is unchanged. So this wasn't a scorer fix; either the original 90.9%/missing-Discounts reading was itself run-specific (a genuine but non-reproducing detection edge case, same shape as the earlier combobox-write-readback-desync false-negative), or it depends on some per-run condition not yet isolated. Only ONE clean confirming run so far -- if 'Discounts missing from Tab Coverage' resurfaces on a future run despite correct field-level behavior, reopen and treat the non-reproduction here as the false negative instead." },
            { id: "verify-at-fill-done-right", owner: "Akuras Kurasa", title: "Sweep fills went unchecked until the separate end-of-run verify pass -- back-and-forth tab revisits to fix a handful of fields", status: "done",
              priority: 1,
              note: "USER-FLAGGED 2026-07-16 directly, with a precise definition: 'consider verify-at-fill to work it has to simply check once after filling, that's it done, we don't navigate back-and-forth and back-and-forth resulting in wasted time loops.' Grounded in real evidence before fixing (per this session's own discipline): run_20260716_025101.txt showed 'Garaging Location'/'Title State' (Vehicle) and 'Collision Deductible' (Coverage) filled once during the main sweep, never checked at that moment, then discovered wrong minutes later by the SEPARATE _verify_pass -- which re-walks all 8 tabs per attempt, so fixing 3 fields cost 3 extra full-form passes (23->2->3->0 corrections) instead of being caught once at the source. FIX (agent.py, sweep's fill call site ~6089): after every sweep fill, do ONE immediate read-back check via _field_matches (same tab, no navigation) -- if it stuck, mark it in a new self._verified_at_fill set (attempt_key-keyed, same identity scheme as _dead_fill_keys); if it didn't, retry ONCE right there before moving on. Wired into _view_mismatches (agent.py ~3399, right next to the existing _dead_fill_keys skip) so the end-of-run verify pass TRUSTS already-fill-verified fields instead of rediscovering them cold. _verified_at_fill cleared on new-record reset alongside its siblings. py_compile + pyflakes clean. LIVE-PROVED on the real _view_mismatches method (no live UI): a field with a genuinely wrong snapshot value is correctly flagged when NOT yet verified-at-fill, and correctly SKIPPED once marked verified-at-fill (even with the same stale-wrong snapshot) -- both PASS. CAVEAT (not yet addressed): only wired into the SWEEP's fill path, not the main per-step OPT2 fill path where most ordinary fills happen -- if back-and-forth persists after this on a live run, that's the next site to extend it to. Also uses the same attempt_key geometry scheme already known to be flip-prone on repeated-section tabs (Driver 2/3) -- lower risk here since Vehicle/Coverage aren't repeated-section tabs, but worth watching. NOT yet live-tested against the real form.\n\nLIVE-TESTED 2026-07-16 (run_20260716_032607.txt) -- CAVEAT CONFIRMED AS A REAL PROBLEM, user furious: 16 verify-pass corrections were mostly CHECKBOXES ('Homeowner', 'Salvage Title', 'ABS Brakes', 'Paperless / e-Delivery'...) filled via the ordinary per-step OPT2/checkbox/combobox paths the sweep-only fix never touches -- exactly the gap flagged above, now proven live. Also: the sweep-side fix itself flagged 'didn't stick first try' on 15 different fields during one run (Bodily Injury, Property Damage, Collision Deductible, DL Number, Accidents (3 yr)...) -- most of these are a genuine, previously-underestimated write-reliability issue (same family as combobox-write-readback-desync) rather than a bug IN the new check itself. REAL FIX (2026-07-16): rather than duplicate the same check at every scattered fill call site (OPT2 type, checkbox toggle, combobox click-fill -- the exact whack-a-mole that took 3 rounds for the tab-lock), built ONE chokepoint in the main step loop instead (agent.py, right after _record_attempt ~line 2180): whatever field was FOCUSED when the step's action was decided is the field that step acted on, regardless of which internal mechanism filled it -- if its post-action value matches the record, mark it verified via the SAME self._verified_at_fill set the sweep-side fix already populates. This covers checkboxes/comboboxes/edit fields uniformly, no matter which of the many fill paths touched them. py_compile + pyflakes clean.\n\nLIVE-TESTED 2026-07-16 (run_20260716_115224.txt) -- STILL NOT ENOUGH: 'Paperless / e-Delivery', 'Vehicle Condition', 'Lienholder/Lender', 'DL Issuing State' and others STILL showed up as verify-pass corrections (14 in pass 1, 5 total passes -- worse than the 4-pass baseline). ROOT CAUSE OF THE MISS: the chokepoint only checked the FOCUSED element -- but checkboxes and click-filled comboboxes often DON'T register a UIA focus change the way typed edit fields do, so the field that was actually just filled was never the one the chokepoint was looking at. FIX: added a SECOND signal alongside focus -- the field actually CLICKED this step, resolved via _elem_at(state, click_position) (agent.py ~2137, already computed earlier in the same loop for a DIFFERENT purpose, focus-inference -- reused here rather than recomputed) then re-resolved in state_after. Checks BOTH the focused element and the clicked element each step; either one matching the record gets marked verified. py_compile + pyflakes clean. PARTIALLY drilled (scratch, ad-hoc -- confirmed _elem_at correctly resolves a non-focused checkbox by click position on the real method) -- the full chokepoint logic is inline in the massive step loop, not a standalone function, so a complete real-code-path drill isn't practical the way the sweep-side fix's drill was. NOT yet live-tested. ALSO REVERTED SAME RUN: the _STALL_LIMIT cut (6->3, see stall-limit-cut node) made things worse, not better -- reverted back to 6.\n\nLIVE-TESTED 2026-07-16 (run_20260716_121508.txt) -- STILL FAILED: 20 corrections in pass 1, 5 total passes -- worse than ever, despite the click+focus chokepoint. THREE separate attempts at making the multi-pass verify loop smarter about not re-flagging good fields all failed live. USER PIVOT (explicit, final): stop trying to make revisits smarter, ELIMINATE them -- 'I want that it won't go back to the other tabs, if it missed it, it missed it.' REAL FIX (2026-07-16, _confirm_finished, agent.py ~5610): added self._verify_ran_once (bool, cleared per-record). _verify_pass still walks all 8 tabs ONCE and fixes what it finds in that single walk-through (that's the one-and-only check, not a revisit) -- but _confirm_finished now submits unconditionally after that ONE call, whether verify_pass returned clean or not, instead of looping back for another full pass when corrections were found. Whatever's still wrong after the single pass is accepted, not chased further. The old CONVERGENCE GATE (same-fields-twice-in-a-row acceptance logic) is now dead code (only one pass ever happens) but left in place, harmless. py_compile + pyflakes clean. LIVE-PROVED on the real _confirm_finished method (no live UI, verify_pass mocked to always report gaps): called twice in a row, verify_pass fired exactly ONCE across both calls, both calls returned True (submit) -- confirms no revisit happens regardless of how many times _confirm_finished gets invoked. This directly delivers the user's stated definition of done. NOTE: this trades completeness for speed by design (user's explicit call) -- a field with a flaky write (combobox-write-readback-desync) will now simply stay wrong if the single pass catches it wrong, no second chance; watch BC score's field-match-rate/value-accuracy on the next real run to see the actual cost of that tradeoff.\n\nSTILL FAILED 2026-07-16 (run_20260716_123115.txt), user interrupted mid-pass: the ONE-PASS version still clicked back into Policy/Policyholder/Vehicle (tabs the main fill loop had already finished) to run its single read-back-and-fix walk -- 'it still returned after already filling a certain tab, I don't want it to do that.' Not a revisit-COUNT problem anymore -- ANY tab-walk after the main fill phase moved past a tab is unwanted, even the first and only one. FINAL FIX (_confirm_finished, agent.py ~5614): _verify_pass is no longer called AT ALL -- once every tab has been visited during the main fill, submit immediately. Whatever the main fill pass produced on each tab is final, no second look for any reason. USER FOLLOW-UP QUESTION (important, answered): 'why was VERIFY a hard-coded process, I said no hard-coded thing whatsoever' -- clarified the distinction: _verify_pass's CODE was never hardcoded in the banned sense (no baked field/tab names, generic label/type/section-driven) but calling it as a FIXED post-fill procedure WAS a hardcoded WHERE-decision -- the agent unconditionally deciding to visit every tab in order, not the transformer -- same category as the already-stripped _try_advance_tab/_focus_first_empty_field WHERE-crutches. Removing the call fixes both complaints at once: no more agent-authored fixed navigation loop, and no more tab revisits. LIVE-PROVED on the real _confirm_finished method (no live UI, _verify_pass mocked to assert-fail if called): called twice in a row, _verify_pass invoked ZERO times, both calls returned True (submit) immediately once tab-coverage was met. py_compile + pyflakes clean. _verify_pass/_verify_ran_once/_verified_at_fill left in the file as dead code (not deleted) in case this trade needs reversing once real accuracy numbers come back. NOT yet live-tested against the real form -- this is the actual final form of the fix, pending confirmation it holds live." },
            { id: "stall-limit-cut", owner: "unassigned", title: "_STALL_LIMIT 6->3 tried and reverted -- more frequent stalls, same total waste", status: "done",
              note: "Speed lever proposed 2026-07-16: 6 'STUCK 6 steps' events in one 115-step run each burned 6 non-productive steps before recovery (jump/sweep) kicked in -- 36 wasted steps, ~2-3min of a ~6.5min main-fill phase. Cut _STALL_LIMIT (agent.py ~886) from 6 to 3 on that reasoning. LIVE-TESTED 2026-07-16 (run_20260716_115224.txt): made it WORSE, not better -- 17 STUCK events fired (vs 6 before) but total duration was UNCHANGED (10.4min vs 10.2min baseline). LESSON: the per-STUCK-event recovery cost (jump computation + sweep) is roughly constant regardless of the trigger threshold -- firing sooner-but-more-often just redistributes the waste, it doesn't reduce it. REVERTED back to 6 same day. The real speed lever is elsewhere (the recovery mechanism's own fixed cost, or per-step overhead independent of stall handling) -- not this threshold. Do not retry this specific lever without a different theory first." },
          ],
        },
        {
          id: "good-data",
          title: "Clean demos → faithful model",
          status: "partial",
          note: "The model can only be as orderly as its training data. Sequence fidelity (Behavioral Match) is a thesis metric.",
          children: [
            { id: "v3-model", title: "v3 model on scrubbed demos (footer clicks removed)", status: "done",
              note: "click_acc 0.945, typing-target 0.85. Section-aware 'attempted' feature baked in." },
            { title: "More demos where the model is weakest (deep tabs, rare moves)", status: "pending",
              note: "Project rule: root fix for mis-prediction = more demos, not more guards." },
          ],
        },
        {
          id: "speed",
          owner: "unassigned",
          title: "SPEED · Sub-1-minute run, not 8-9 minutes",
          status: "partial",
          priority: 1,
          note: "GOAL (user-set 2026-07-16): 'ideally 1-5 minutes' per record; currently 8.5-9.5 min end-to-end (start-to-submit), down from a 14-21 min baseline. Real levers landed so far: STEP_DELAY 1.5s->0.7s ([[step-delay-cut]]), verify-skip-clean-tabs (don't re-scan settled tabs), verify-never-converges-hallucination short-circuit (cut wasted LLM round-trips per tab), sweep-trusts-write-path-confirmation (removed false verify-at-fill retries from a redundant snapshot re-check, [[combobox-write-readback-desync]]). REMAINING, NOT YET ATTEMPTED: the temporary observe()-call-count instrumentation added 2026-07-16 (agent.py _observe() + step-header log, marked TEMP, not yet removed) surfaced that some single steps cost 40+ observe() calls (the sweep filling ~20 fields in one un-tallied step) -- batching multiple sweep fills into fewer observe() round-trips is the next real architecture-level lever (not another delay tweak), same conclusion the run-x5 node already flagged ('sub-1-min needs batch fills instead of observe-per-step'). _STALL_LIMIT tuning already tried and reverted (6->3 made it worse, not better, see [[stall-limit-cut]]) -- do not retry that lever without a new theory. Do NOT conflate this with [[verb-loop-rewrite]] -- that's an architecture/correctness cleanup, only a possible speed side-effect, not a targeted speed fix.",
        },
        {
          id: "verify-at-fill",
          owner: "Ralph",
          title: "Verification doesn't re-check what's already confirmed",
          status: "done",
          note: "IMPLEMENTED 2026-07-10, live-exercised in the ×2 probe 2026-07-11: both records ran the gated verify and submitted — no wedge, no missed-fill regression. Mechanism: LLM completeness call fires only when a view shows an EMPTY live fillable the deterministic branch couldn't settle; filled views skip straight to the next scroll. Deterministic clobber-catch + convergence gate unchanged. Caveat: wall-time saving not explicitly measured.",
        },
        {
          id: "scroll-no-ops",
          owner: "unassigned",
          title: "ScrollPattern sometimes reports success but the panel doesn't move",
          status: "risk",
          priority: 2,
          note: "RE-PRIORITIZED 2026-07-14 (was 'pending', low-priority-by-neglect — now 'risk', P2): live-observed causing an ACTUAL never-converging verify loop, not just a cosmetic warning. Record-10 run: verify_pass 'corrected' 29 fields across 6 tabs at step 238, then ran a FULL fresh 8-tab verify pass again at step 239 and found MORE to fix. User-observed live: 'it was in a loop', run manually killed at step 241. ROOT CAUSE FOUND same day — see verify-checkbox-identity-unified: nearly all 29 corrected fields were checkboxes, and the read path used a different (weaker) identity resolution than the write path. Fixed there. ScrollPattern no-ops may still be a separate contributing factor — not yet independently root-caused, revisit if the identity fix doesn't fully close the loop." },
        {
          id: "verify-checkbox-identity-unified",
          owner: "Ralph",
          title: "Verify's checkbox read used weaker identity than the write",
          status: "done",
          note: "FOUND + FIXED 2026-07-14, root cause of the 29-fields-corrected-every-pass loop above: _is_checked (verify's checkbox-state reader) did its own by-NAME-ONLY UIA lookup, with no geometry disambiguation — unlike _act_on_element (the WRITE path), which resolves via _resolve_live_control (name + nearest-bbox-center match among same-named twins, built 2026-07-09 for repeated-section fields). If the by-name-only read ever landed on a different UIA node than the one actually written to (wx keeps hidden tab pages' controls in the tree too), verify would see a stale/wrong toggle state and re-'fix' an already-correct checkbox every single pass, forever — exactly matching the observed symptom. FIX: _is_checked now tries _resolve_live_control FIRST (same resolver the write path uses), only falling back to the old by-name search if that fails. Closes the already-filed 'verify-fix read-back unification' residual (from identity-everywhere, 2026-07-13). Cannot be offline-drilled (live UIA identity-resolution discrepancy). LIVE-CONFIRMED: next uninterrupted run converged 12->4->0 corrections, naturally terminated, 100% completion — no more repeat-forever loop.",
        },
        {
          id: "verify-llm-fallback-absent-guard",
          owner: "Ralph",
          title: "Verify's LLM-fallback wastes calls re-asking about absent fields",
          status: "partial",
          note: "FOUND 2026-07-14 on the same clean run that confirmed the checkbox fix: 10 of 50 LLM calls (20%) were repeated 'LLM-fix Claim Number' asks — record 10 is claims-free, genuinely absent, and the sweep already has an inline 'record is source of truth, stays blank' guard for this (built 2026-07-11) — but it lives DOWNSTREAM of _navigation_protocol, and the bucket-2 short-circuit only short-circuited on a RESOLVED value, not a CONFIRMED-absent one, so absent fields still fell through to a full LLM round-trip every time verify's LLM-fallback branch called this function. FIX: the short-circuit now also returns a direct blank fill when the record is loaded but genuinely has nothing for the field — same design rule as the sweep's existing guard, just applied before the LLM call instead of after. Syntax-checked; NOT yet live-retested." },
        {
          id: "verify-scroll-to-top-fix",
          owner: "Ralph",
          title: "Tab switches don't scroll to top -- top-of-tab fields (Vehicle spec block) go completely unfilled",
          status: "partial",
          priority: 1,
          note: "d2_dl_exp kept getting Driver 3's/Policyholder's value on 6 separate live reruns. ROOT CAUSE: if a tab's scroll position carried over from an earlier visit, verify's FIRST scan could land mid-way through a repeated section's block (Driver 2's header already scrolled past, only Driver 3's visible below) -- _detect_section correctly returns '' by the letter of the code (nothing above) but WRONG semantically (Driver 2's header exists, just not in this view). FIX: _verify_pass now calls _scroll_form_to_top before its first scan of each tab. METHODOLOGY CHANGE (user-driven, 2026-07-14): instead of a 15-20 min full live run to check this, wrote scratch/probe_verify_scroll_to_top.py -- attaches to the ALREADY-OPEN form, clicks straight to Drivers via SelectionItemPattern, no agent/LLM at all, checks whether both section headers land on-screen. CONFIRMED cheaply: both visible immediately on tab-open here, no scroll even needed. This proves the mechanical premise, NOT the full live-agent case (a tab re-visited mid-scroll from an earlier pass) -- still needs one real run to fully close out, but de-risked first instead of guessing blind.\n\nRESURFACED 2026-07-16 (run_20260716_170410.txt), MUCH BIGGER IMPACT THAN ORIGINALLY SCOPED: investigating why Field Match Rate is only 66.9% (54/163 fields blank), found nearly the ENTIRE Vehicle-tab spec block missing -- VIN, Year, Make, Model, Trim, Body, Color, Doors, Cylinders, Displacement (10 fields) plus 6 safety checkboxes that should be checked (anti-theft/airbags/ABS/daytime-lights/backup-camera/lane-assist), ph_homeowner, cov_um_uim, cov_acc_forgive. Read the log around the Vehicle-tab switch (line 774): the very first field the transformer interacted with after switching to Vehicle was 'Fuel Type' (a mid-page field) -- it NEVER touched VIN/Year/Make/etc. at the top. ROOT CAUSE CONFIRMED: this is the EXACT SAME bug as the original 2026-07-14 finding above, just hitting a DIFFERENT, now-live code path. The original fix only patched _verify_pass (agent.py ~5374) -- which is now fully dead code (removed same day, no revisit policy) -- so the scroll-to-top safeguard it provided no longer runs ANYWHERE in the live path. Grepped all 4 places a tab switch actually happens (the GAP-path's forced-unvisited-tab click ~1279, the GAP-path's normal LLM-driven tab click ~1296, _try_advance_tab's stuck-guard advance ~4693, and the sweep's two switch sites ~6254/~6283) -- NONE of them ever called _scroll_form_to_top. wx's ScrolledPanel keeps whatever scroll offset it last had; if that's mid-page when a tab is (re-)entered, the top section is simply invisible and never gets discovered this visit. FIX (agent.py, all 5 tab-switch call sites): added self._scroll_form_to_top(self._observe()) immediately after every tab-switch click, before the fresh tab's fields get processed -- generic, no hardcode (doesn't reference any field/tab name), matches the HOW-mechanics division of labor. py_compile + pyflakes clean (ast.parse OK, zero F821). NOT yet live-tested -- next run should show the Vehicle-tab top section (and any other tab's top section) getting filled instead of skipped.\n\nLIVE-TESTED 2026-07-16 (run_20260716_172320.txt) -- FIX FIRED, HYPOTHESIS WRONG: 'Scroll-form-top' logged 8 times across the run, confirmed firing right after the Vehicle-tab switch (17:26:04 click -> 17:26:06 scroll-to-top). But the VERY NEXT step still went straight to 'Fuel Type' (mid-page, same y=628 as every prior run) -- the missing top-of-tab fields were NOT hidden by a stale scroll position; scroll-to-top put them on-screen and the transformer's own pointer still skipped straight past them. Field Match Rate barely moved (65.0% vs 65.6%), confirming nothing changed. Checked the form source (car_insurance_entry/car_insurance_form_wx.py:395-424): 'Fuel Type' is the 3rd field of the SECOND section ('Engine & Drivetrain') -- the transformer's learned click-order starts there, never targeting ANY of the 8 fields in 'Vehicle Identification' (VIN/Year/Make/Model/Trim/Body/Color/Doors) or the first 2 of 'Engine & Drivetrain' (Cylinders/Displacement) at all. RETRACTED: this is NOT a scroll/reveal bug for the Vehicle tab -- it's a genuine MODEL/TRAINING-DATA gap, exactly the project's own documented rule ('root fix for fixation/mis-prediction = more demos, not more guards'). The demos this model trained on likely never demonstrated filling Vehicle Identification's fields, so the transformer never learned to click them. The scroll-to-top fix itself is KEPT (harmless, mechanically correct, may still help other tabs/cases with a genuine scroll-carryover) but is NOT the fix for the missing-Vehicle-fields problem. REAL FIX NEEDED: record demos that start Vehicle tab at VIN and fill through Vehicle Identification + the front of Engine & Drivetrain before Fuel Type, then retrain -- a data problem, not a code problem. Status stays partial: the scroll mechanism is done and harmless; the actual missing-fields root cause is now correctly identified but unaddressed.\n\nCOVERAGE GAP FOUND + CLOSED 2026-07-18, during the verb-loop rewrite's SWITCH_TAB verb audit: grepping for every tab-switch site turned up THREE more that the 2026-07-16 pass missed -- the '(fixation) page done -> switch tab' handler (~2113), its escalation-fallback sibling (~2130), and, most notably, a GENERIC 'any click landed on a tab element' detector (~2208) that catches the TRANSFORMER'S OWN organic tab clicks -- separate from GAP/fixation/sweep/_try_advance_tab, all of which `continue` before ever reaching this generic block. This generic site had a comment literally describing the missing behavior ('mark a tab switch so the next step scrolls to top') that was never actually implemented -- _tab_just_switched was set but the only place that ever read it was a debug log line, never a scroll trigger. Added self._scroll_form_to_top(self._observe()) to all three. Total tab-switch coverage is now 8 sites (7 named + this 1 generic), confirmed via a final _tab_elems_now( grep that every remaining call site is either already covered or dead code (_verify_pass). py_compile + pyflakes clean. NOT yet live-tested." },
        {
          id: "five-records",
          owner: "Ralph",
          title: "Ten records in a row",
          status: "partial",
          priority: 5,
          deps: ["acceptance-run", "trust-numbers"],
          note: "The scope's Definition of Done — RAISED from ×5 to ×10 (2026-07-14) to match the full 10-record intake file. ×2 probe PASSED 2026-07-11: both records submitted, record 2 filled with record-2 data, reset clean, scorecards archived (record-2 mechanics best yet: 3.7% waste, 1.9 steps/field, 96.3% exec success). 2026-07-14: ×10 attempt got 9/10 CLEAN (see run-x5) — huge validation of the day's crosstalk fixes, but flagged a real speed problem (14-21 min/record) now being tackled. Subtasks below = the probe's findings. (node id kept as 'five-records' for existing dep-chain compatibility.)",
          children: [
            { id: "record-bounds", title: "Blank fields don't inherit record 1's values", status: "done",
              note: "CONFIRMED LIVE 2026-07-11 (second --start_record 2 probe): every claim-field peek returned nothing, cache stayed at record 2's 135 fields, zero record-1 values. Two fix rounds: (1) strict record bound at five blob-parse sites; (2) _record_line_span — all line searches (_peek_notepad, NotepadDataSource.peek/peek_next_after/refresh) bounded to record N's slice, absent record = search NOTHING. LESSON: audits find what you suspect, probes find what you don't." },
            { id: "blank-fills", title: "Absent from record = leave blank (nobody gets to fill it)", status: "partial",
              note: "FOUR fillers, closed one probe at a time: (1) LLM invention on empty lookup → absent=skip, no LLM call — VERIFIED 17:53; (2) merge's TextResolver rescued the empty text from visible Notepad (= record 1) → skip_field flag, resolver not consulted — VERIFIED 18:00; (3) the SWEEP typed LLM junk literally via identity executor → sweep guard: RECORD IS SOURCE OF TRUTH — no record value = stays blank (settled); record value present = beats the LLM proposal. NO literal blacklist (user-flagged: ruleset-inference territory); (4) verify inherits via dead-key exclusion. 18:24 probe: skip machinery PASSED wholesale (every absent field skipped+honored, zero junk, LLM% 16.7%); sweep guard itself unexercised (OPT2 skips settled the tab before sweep fired — same lookup logic, low risk, watched naturally next full run). Residuals: model re-picks skipped fields until STUCK (cheap); annotated blanks ('(N/A — no collision coverage)') typed literally = ruleset-inference case, filed there." },
            { id: "run-x5", title: "The full ×10 run, scorecards archived", status: "done",
              deps: ["record-bounds", "trust-numbers"],
              note: "RAISED from ×5 to ×10 (2026-07-14) — the intake file has 10 records available, use all of them. FIRST ATTEMPT 2026-07-14 (run_20260714_121233.txt): records 1-9 ALL completed — 100% task completion, natural termination, ZERO crosstalk mismatches on every single one. Strongest validation yet of the day's 5-round driver-crosstalk fix chain, holding across 9 different records. Record 10 interrupted by the user at step 0 (huge gap — came back ~4h later, immediate Ctrl+C); the stale-submission guard correctly refused to fake-grade it. USER CALL 2026-07-14: 9/10 clean accepted as sufficient proof of the DoD — not a literal 10/10, but the crosstalk-correctness claim this run was meant to validate is fully demonstrated. Field Match Rate 58.5%-76.2% across the 9 (separate, already-tracked gold-coverage gap, not a regression). SPEED FLAGGED separately: 14-21 min/record, 59-121 LLM calls/record — fix built same day (see sweep-deterministic-nav), not yet live-retested. (node id kept as 'run-x5' for dep-chain compatibility.)" },
          ],
        },
        {
          id: "self-improving",
          owner: "unassigned",
          title: "It learns its own rules from runs",
          status: "pending",
          priority: 6,
          note: "Ruleset-inference loop is OPEN: inferred rules (e.g. 'leave blank means skip') never reach the LLM. Close extract() → ruleset.md → prompt. Fresh case (2026-07-11): 'Deductible ($)' typed the literal '(N/A — no collision coverage)' straight from the record — 'n/a + annotation = leave empty' is a learnable rule, deliberately NOT hardcoded.",
        },
          ],
        },
      ],
    },
    {
      id: "verb-loop-rewrite",
      owner: "Ralph",
      title: "VERB-LOOP REWRITE · One dispatch, one way to confirm success",
      status: "next",
      priority: 1,
      note: "SCOPED 2026-07-16 (git branch 'verb-loop-rewrite'). Problem: the main step loop is one 400+ line function deciding what to do by checking ~15 separate ad-hoc sets/dicts invented one bug at a time (_dead_fill_keys, _attempted_keys, _fixation_hits, _filled_this_tab, _checked_fields, _nochange_click_pos, _keystroke_retried, _reveal_focus_count, _verified_at_fill...) — they overlap in purpose without sharing logic, which is exactly why bugs like today's sweep-re-verify desync happen: two different code paths independently 'verify' the same fill in two inconsistent ways. GOAL: replace the pile of trackers with ONE per-field state record (tried-count / dead-flag / last-verified-value, keyed by _attempt_key) and a small set of explicit VERBS (CLICK / TYPE / CHECK / SCROLL / SWITCH_TAB / SUBMIT), each self-confirming success the way the combobox rung-4 ladder already correctly does (read back off the SAME live ctrl right after acting, not a second independent snapshot re-check). STAGE 1 (audit) DONE 2026-07-16: enumerated every self._[a-z_]* state var in __init__, cross-referenced real call sites (not just declarations). DEAD (write-only, safe to delete): _verify_fix_count, _verify_dead_stable, _verify_clean_tabs, _verify_ran_once, _verified_at_fill -- all existed only to serve _verify_pass/_view_mismatches, both already confirmed fully dead (never called) earlier the same day. ALIVE, load-bearing, must be preserved or folded into the new per-field record: _dead_fill_keys (23 refs), _attempted_keys, _visited_tabs, _fixation_hits, _filled_this_tab, _checked_fields, _nochange_click_pos, _current_tab_idx, _start_tab_idx, _tabs_total, _keystroke_retried, _reveal_focus_count (last two confirmed genuine read+write retry-limit gates, not write-only). Config/mode flags out of scope: _pure_transformer, _no_autohandlers, _use_ocr, _locked_title, _source_window, _guidance, _task_name, _system_prompt. NOT YET DONE: Stage 2 (concrete verb-dispatch design + migration order) and the actual rewrite. EXPLICIT RISK NOTED to the user before starting: this touches the main loop of a 7000+ line file with a long trail of hard-won LIVE-TESTED fixes sitting in that exact loop (submit-lock, tab-lock, checkbox record-lookup, leave-blank guard, combobox rungs) -- doing this freehand in one pass risks silently unwinding one of them with no live run to catch it until the next test. PLAN: migrate one verb at a time (CLICK first, since it's the one behind today's exact sweep-re-verify bug), live-test each before touching the next, isolated on this branch.\n\nSTAGE 2, FIRST SLICE DONE 2026-07-16: found THREE independent checkbox-toggle implementations in agent.py -- _act_on_element's UIA TogglePattern version (canonical: resolves ctrl, no-ops if already correct, toggles + self-confirms via read-back off the SAME control), the main per-step 'click landed on a checkbox' handler (~1868, hand-rolled Win32 BM_SETCHECK/BM_GETCHECK, the one fixed for record-awareness earlier today), and a 'type intercept' path (~1800, unconfirmed fire-and-forget BM_SETCHECK). Migrated the per-step click handler to call self._act_on_element(_chk_at_cp, 'yes') instead of hand-rolling Win32 calls -- removes one duplicate implementation, unifies its confirm logic with the one the sweep already trusts. Dead vars cleaned (_chk_cx/_chk_cy, no longer needed once WindowFromPoint was dropped). py_compile + pyflakes clean (zero F821, zero unused). Condition-drilled (not unit-testable in isolation, inline in the 400+-line step loop): record-NO leaves unchecked no-op (unchanged), record-YES calls the canonical verb which itself handles both 'needs toggle' and 'already correct' cases uniformly. NOT yet live-tested. REMAINING: the 'type intercept' path (~1800) still hand-rolls its own BM_SETCHECK with no read-back confirm -- next slice.\n\nSECOND SLICE DONE 2026-07-16: migrated the 'type intercept' path too (fires when the LLM types a truthy value into a field that turns out to be a checkbox) -- was an unconfirmed, fire-and-forget Win32 BM_SETCHECK with zero read-back check at all (worse than the per-step click handler's version, which at least read GETCHECK back). Now calls self._act_on_element(_fel, 'yes'), same canonical verb as the other two call sites. All THREE per-step/sweep checkbox-toggle call sites in the main step loop now route through the one canonical verb; only remaining hand-rolled checkbox code left is deep inside _nav_fill_field's own legacy fallback (~6775), which is unreachable in practice since _nav_fill_field calls _act_on_element FIRST and only falls through to it if UIA control resolution itself fails entirely -- deprioritized, true last-resort dead-path territory, not a live duplicate. py_compile + pyflakes clean.\n\nLIVE-CONFIRMED 2026-07-16 (run_20260716_170410.txt): both checkbox-verb call sites fired live and worked -- 'Paperless / e-Delivery'/'E-Signature Obtained'/'Auto-Pay Enrolled' checked via the type-intercept path (second slice), 'Renewal Policy'/'Rental Reimbursement'/'Good Driver'/'Multi-Policy'/'Loyalty Discount' checked via the click-handler path (first slice) -- all confirmed correctly via the shared canonical verb, zero force-check errors, zero verify-at-fill retries, zero errors/tracebacks. Value Accuracy 100% (60/60), Tab Coverage 100%, Field Match Rate 66.9% (up slightly from 65.6%), Duration 8.5min (stable). CHECK-verb unification is done and proven live across both call sites. STOPPING POINT for today -- TYPE/generic-CLICK/SCROLL/SWITCH_TAB/SUBMIT verbs and the ~9-tracker consolidation are still unstarted.\n\nTYPE VERB AUDITED (2026-07-16): unlike CHECK, found NO live duplication bug -- the main per-step path has exactly ONE dispatch site (self._executor.execute(prediction), line ~2187, all typed values funnel through it) and the sweep/nav-fill path already has a clean canonical-then-fallback structure (_nav_fill_field tries _act_on_element's self-confirming write first, only falls to a raw-paste legacy path if that fails) -- same shape CHECK's fix produced, already in place from an earlier session (the 2026-07-11 'SINGLE FILL PIPELINE, identity-everywhere' inversion). Forcing a TYPE-verb rewrite here would mean restructuring the CORE per-step decision loop for no found bug -- explicitly NOT done, matches the project's own 'no premature abstraction' rule. Moved to combobox instead.\n\nCOMBOBOX SLICE DONE 2026-07-16: found a real duplication -- TWO independent hand-rolled 'click combobox open, wait for visible listitemcontrol children, click the matching one' legacy fallbacks, one inside _fill_element (~6440, used by the main OPT2 per-step path) and one inside _nav_fill_field (~6839, used by the sweep) -- both only reached when _act_on_element's canonical UIA rung-4 write fails, but independently written with DRIFTED matching logic: only _fill_element's copy used the bidirectional startswith check, _nav_fill_field's copy only did exact-match-then-one-direction-substring. This is exactly why only ONE of today's earlier apostrophe-insensitive / numeric-exact-match fixes (both applied to _act_on_element's rung-4 matcher) could ever have been needed there and not duplicated into these fallbacks -- they'd already drifted into two separate untested copies. FIX: extracted both into one shared helper, _combobox_legacy_click_fallback(elem, value) (agent.py, right before _fill_element) -- same click+wait+match+click logic, kept the more permissive bidirectional matcher. _fill_element's inline block now calls it directly; _nav_fill_field's inline block calls it then keeps its own bookkeeping (_filled_this_tab/_mark_attempted) around the call, since _fill_element's callers already do that bookkeeping themselves upstream instead. py_compile + pyflakes clean (ast.parse OK, zero F821, no new unused vars). NOT yet live-tested -- this only changes the LAST-RESORT fallback path (reached when the canonical rung-4 write already failed), so live risk is lower than the CHECK-verb changes, but still unverified against a real run.\n\nLIVE-TESTED 2026-07-18 (run_20260718_121434.txt): clean run, zero errors, zero verify-at-fill retries. Every single combobox fill (Policy Term, Policy Status, Gender x2, Marital Status, Occupation, Education Level, State x3, Fuel Type, Transmission, Drive Type, Primary Use, Garaging Location, Vehicle Condition, Coverage limits, Payment Frequency x2, Claim Type/Status, Relationship x2, Method...) succeeded via the canonical rung-4 path -- the legacy fallback (this slice's edit) was never triggered even once, so it caused no regression but also remains functionally unexercised live. Field Match Rate jumped to 73.0% (119/163, up from 65-67% baseline) -- normal run-to-run model-order variance plus cumulative effect of today's checkbox fixes, not attributable to this specific slice (never fired). Vehicle Identification gap CONFIRMED STABLE across three separate runs now (same 10 spec fields + same safety checkboxes missing every time, identical to the prior two runs) -- this is a reproducible model/training-data hole, not noise, exactly as diagnosed. Duration 8.3min (stable), Value Accuracy 99.2% of filled." },
    {
      id: "generalization",
      owner: "Ralph",
      title: "GENERALIZATION · One engine, any app",
      status: "partial",
      note: "The organ that makes Intern travel beyond this form: swap the EYES (perception seam: UIA today, vision/Excel adapters proven at the same seam), swap the SOURCE (DataSource seam: Notepad today, any key:value provider), keep the same learned WHERE + WHAT-resolver stack (deterministic lookup → inferred rules → LLM judgment) + fill pipeline. Scope 2 tests that the stack travels; Scope 3 tests the judgment resolver. Windows-wide today; beyond Windows = the vision branch below.",
      children: [
    {
      id: "perception",
      title: "Perception (swappable eyes)",
      status: "partial",
      note: "The seam itself: same element schema (bbox/type/label/value) regardless of how the screen is READ. UIA (accessibility tree) is Scope 1's implementation — proven, live. Vision (CV+OCR / learned parser) and the Excel adapter are proven at the same seam. This is what makes Scope 2+ possible without touching the agent's WHERE/WHAT logic.",
      children: [
    {
      id: "vision",
      title: "Vision (pixels, not APIs)",
      status: "partial",
      note: "Big Three #1 — CV+OCR observer (PR #8): screenshot → boxes + text → same element schema. Proven: agent NAVIGATES from pixels (2026-07-10 probe, 7/7 on-target clicks); tooling: per-observation flipbook + live viewer (make see). Not yet: filling. This is the generalization organ — Scope #2+ needs it; Scope #1 stays on UIA.",
      children: [
        { id: "vision-focus", title: "It knows which field it's in (focus inference)", status: "risk",
          note: "THE BLOCKER: pixels expose no keyboard focus → the fill trigger ('focused empty field') never fires → navigates forever, types nothing (live-observed). DO: agent-side inferred focus = last clicked fillable when observer reports None; later, visual caret detection." },
        { id: "vision-labels", title: "It reads labels whole, not in pieces", status: "done",
          note: "FIXED (424beea): whole-line assembly via Tesseract line ids + punctuation cleanup — all 10 Tab-1 labels whole/exact; identity-executor fills landed on clean labels. Residual: label/value bleed on filled fields (goes away with the learned parser)." },
        { id: "vision-identity", title: "It knows a field is the SAME field across frames", status: "done",
          note: "FIXED (819a4a7): detector ids were per-frame detection order — consecutive frames named different fields with the same id, so fills were verified against the WRONG element and dead-marked. Frame-to-frame tracker (label + nearest center) carries ids; parser-agnostic. Unit-verified; needs one live probe to confirm read-back." },
        { title: "It ignores window chrome", status: "pending",
          note: "Caption-bar buttons detected as checkboxes — the agent clicked 2px from CLOSE. DO: exclude the captured window's title-bar band from detection (by caption height, not pixels)." },
        { title: "It tells dropdowns from text fields", status: "pending",
          note: "0 comboboxes detected on a tab with 3 → wrong fill mechanics chosen. DO: detect the dropdown arrow glyph at the right edge of the box." },
        { id: "alt-vlm", title: "Find alternative VLM", status: "pending",
          note: "Evaluate vision-language / screen-parsing models for the perception seam. Candidates: OmniParser v2, ShowUI, UGround, Qwen2.5-VL — local preferred (human-like control rule). DO: benchmark each behind the same detect_elements() seam; accept when perception_eval beats the current classical baseline on box precision/recall + label accuracy. Overlaps vision-parser — this is the model-selection half." },
        { id: "vision-parser", title: "Real fix: learned screen parser replaces hand-rolled CV", status: "pending",
          note: "The classical detector needs a new heuristic per widget style — it will never generalize (guard-pile anti-pattern, perception edition). DO: swap cv_detector's core for a pretrained screen-parsing model (OmniParser-class, local ONNX; evaluate ShowUI/UGround) behind the SAME detect_elements() seam; keep Tesseract for values; accept when perception_eval beats the classical baseline on precision/recall + label accuracy." },
        { title: "Vision demos recorded + vision model trained", status: "pending",
          deps: ["vision-focus", "vision-labels"],
          note: "DO (after the two blockers): record with --perception vision, clean, train — the model that needs no accessibility tree at all." },
      ],
    },
      ],
    },
      ],
    },
    {
      id: "scope2",
      owner: "unassigned",
      title: "SCOPE 2 · Cross-App Transfer (web form → Excel)",
      status: "pending",
      deps: ["acceptance-run", "five-records", "vision"],
      note: "Proof scope #2: web form → Excel. Same engine, different eyes and hands. Perception swap already PROVEN (ExcelObserver speaks the same schema).",
      children: [
        { title: "Excel eyes: read live sheets reliably", status: "partial",
          note: "DO: run ExcelObserver against real workbooks; verify cell text, formulas, coordinates and sheet names land in the canonical element schema. The swap was proven once in isolation (2026-06-09) — needs endurance testing, not design." },
        { title: "Excel hands: target cells, enter values", status: "pending",
          note: "DO: implement cell targeting (click or arrow-key navigation to the active cell) + value entry, as identity-executor-style actions. Decide click-per-cell vs keyboard navigation by recording how the human actually does it." },
        { title: "15-20 recorded transfer demos", status: "pending",
          note: "DO: record with app/main.py — one pass = read a web-form record, enter it into the Excel template. Same recorder, new out_dir (e.g. data/demos/excel_transfer). NO duplicate button; clean with clean_demos.py after." },
        { title: "Trained + fidelity measured", status: "pending",
          note: "DO: train a task-specific checkpoint on the transfer demos (make train-semantic TRACE_DIR=...), then a live run scored against the source records. Pass = same bar as Scope #1: unattended, correct values, demonstrated order." },
      ],
    },
    {
      id: "scope3",
      owner: "unassigned",
      title: "SCOPE 3 · Judgment Cloning (email/ticket triage)",
      status: "pending",
      deps: ["scope2"],
      note: "Proof scope #3: email/ticket triage — two users triage DIFFERENTLY, and the agent reproduces its user's calls. The claim no record-and-replay tool can make.",
      children: [
        { title: "Demos with decisions in them (varied inputs → varied responses)", status: "pending",
          note: "DO: design + record a triage task where the INPUT varies and the demonstrated RESPONSE varies with it (e.g. active policy → reply A, lapsed → warning, missing fields → inquiry). Keep every decision inferable from VISIBLE content — no hidden intent. This dataset design is the hard part; do it before any code." },
        { title: "Learn the branching (what condition drives which action)", status: "pending",
          note: "DO: build the workflow-induction pass — compare traces across cases, find the state features that predict which action branch was taken, emit an explicit branching spec (JSON/MD). Then measure: does the trained agent take the USER'S branch on held-out cases?" },
        { id: "dagger", title: "Learn from corrections (DAgger)", status: "risk",
          note: "CorrectionHandler currently captures 0 steps in live runs — the correction loop is wired but dead. Blocks cold-start improvement." },
      ],
    },
    {
      id: "cleanup",
      title: "Clean up Intern codebase",
      status: "pending",
      note: "General maintenance pass — not tied to one scope. Candidates: retire the pixel/paste fallbacks now that the fill pipeline (_fill_element) exists as the primary path; prune stale scratch/ probes and superseded demo dirs; consolidate the value-lookup fallback chain (5 sites touched during the contamination fix — verify none are now dead code); remove old guard-pile leftovers replaced by the identity-executor + verify-at-fill work. DO: scope this properly before starting — audit first, then decide what's actually safe to delete vs what's load-bearing.",
    },
    {
      id: "thesis",
      owner: "Daniel",
      title: "THESIS PAPER",
      status: "pending",
      note: "The academic deliverable: the three scopes turned into chapters, metrics and benchmark comparisons. Nothing here starts producing text until Scope #1 has trustworthy numbers.",
      children: [
        { id: "revise-ch1", title: "Revise Chapter 1", status: "pending", owner: "Daniel",
          note: "DO: pass over Chapter 1 (introduction/problem statement) for clarity, structure, and consistency with the current project framing." },
        { id: "revise-ch2", title: "Revise Chapter 2", status: "pending", owner: "Daniel",
          note: "DO: pass over Chapter 2 (background/related work) for clarity, structure, and consistency with the current project framing." },
        { id: "revise-ch3", title: "Revise Chapter 3 (Methodology + evaluation)", status: "pending",
          owner: "Daniel", deps: ["scope1"],
          note: "DO: write/revise ch.3 (architecture: perception adapter → transformer WHERE → LLM WHAT → identity-executor HOW; BC training recipe) and ch.4 (metrics + runs) — most affected by the recent work (fill pipeline, deterministic short-circuit, scorer fixes). Blocked on Scope #1 numbers being real — which needs the trusted scorer first." },
        { title: "Benchmarks vs generic agents (OS-World)", status: "pending",
          note: "DO: pick 2-3 comparable form/transfer tasks from OS-World (or equivalent), run a generic agent and Intern on both, compare completion + fidelity + cost-per-run. The claim to support: personalization + local execution beat generic agents on repeated personal workflows." },
        { id: "behavioral-match", title: "Sequence-fidelity metric actually measures", status: "done",
          note: "FIXED 2026-07-12: reference wired to the training corpus. FOUND STILL BROKEN 2026-07-13 (dead empty dir shadowed the real corpus via plain .exists()) → fixed same day (check for session_* subfolders). LIVE-CONFIRMED 2026-07-13 rerun: Behavioral Match printed a REAL number — 13.5% (seq_sim 13.5%, tab_order 0%, agent=90 actions vs ref=250). CALIBRATION: demo-vs-demo similarity = 81% is the human ceiling — 13.5% is real signal, low because the agent's single-record run (90 actions) is far shorter than the multi-tab reference session (250 actions) and tab-visit order isn't yet optimized for sequence match. The metric MOVES now — that was the fix's whole job. Improving the number itself is a separate, later task (train on re-cleaned demos / align tab order)." },
      ],
    },
  ],
};
